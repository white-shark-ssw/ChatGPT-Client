# DEV-protocol-read

## Status

**Completed — b7 runtime gate accepted; durable docs/merge in progress**

- **Work ID**: `DEV-protocol-read`
- **Routing aliases / keywords**: `协议读取 / 会话列表 / 会话详情 / conversation list / conversation detail / protocol read`
- **Task**: Establish current ChatGPT conversation-list and conversation-detail protocol evidence using the merged, runtime-tested authentication/account-context baseline before production conversation models or native read UI are implemented.
- **Acceptance result**: Passed on authoritative candidate `DEV-protocol-read-0.1.0-b7` on iPhone / iOS 17.0. Screenshot title `会话列表 · 会话详情通过` and exported diagnostics agree.
- **Baseline / branch / PR**: `main` remained `5547b32ed8c794052eb80bb751abf33121b62926`; branch `dev/protocol-read-20260826`; PR #7 open/draft/mergeable before completion update. Exact b7 product/workflow source is `44a137b973e29e2a313e9114fdacb7727dccefb9`; later branch commits through the pre-runtime-doc head were docs-only.
- **Candidate / artifact**: `DEV-protocol-read-0.1.0-b7`, version `0.1.0 (7)`; authoritative push run `32938912018`; artifact ID `9595827498`; IPA `ChatGPTClient-0.1.0-b7-dev-protocol-read.ipa`; IPA SHA-256 `64b0cc055bc9da27bc887698ba18ae5cb2cc0fdb9f15a3a59eb09e55c5fcb4ae`; ZIP digest `sha256:c1d851dc949a43587f94fffd34b35c233ff5f35a2c8eef3399d2e722a9f7833f`.
- **Runtime evidence source**: User export `ChatGPTClient-Diagnostics-20260826-070458.json` reports candidate b7, build 7, source `44a137b973e2`, Release, iPhone, iOS 17.0.
- **Authentication behavior during b7**: First account-context attempt used 46 total / 27 matched WebKit cookies and `/api/auth/session` returned HTTP 403 (`stage=session`, `not_available`). User explicitly pressed `重新开始`. Second attempt used 49 / 30 cookies, `/api/auth/session` returned HTTP 200, accounts-check returned HTTP 200, selected the accepted `plus` / `personal` context, and ended `status=ok` in 1583.83 ms. No automatic retry occurred; this reinforces the existing challenge-sensitive auth contract.
- **Conversation-list runtime result**: One authenticated `GET /backend-api/conversations?offset=0&limit=28&order=updated` returned HTTP 200, 23,697 bytes, `itemCount=28`, `totalCount=29`, `responseLimit=28`, `responseOffset=0` using only the accepted transient bearer + copied ephemeral WebKit cookies. No `chatgpt-account-id` or browser-only headers were added.
- **Conversation-detail runtime result**: First returned conversation detail returned HTTP 200, 13,152,411 bytes, `mappingCount=2068`, `messageNodeCount=2067`, `nullMessageNodeCount=1`, `rootNodeCount=1`, `branchingNodeCount=3`, `maxChildrenCount=2`, `contentTypeCount=6`; role counts were user 22 / assistant 1235 / tool 810 / system 0 / other 0 and sum exactly to all 2067 message nodes. `currentNodePresent=true`, `currentNodeMapped=true`, `conversationIdentityPresent=true`, `conversationIdentityMatches=true`.
- **Protocol probe timing**: `conversationReadProbe.end status=ok`, `stage=detail`, total duration 13,573.66 ms. This is end-to-end probe time; the current diagnostics do not separately attribute network, JSON parse, or structural inspection time.
- **Validation state**: **Code written + CI passed + Artifact produced + Runtime/manual/real-device tested.** Accepted for the exact current Plus/personal conversation-list + one-detail read scope on iPhone / iOS 17.0. Not evidence for send/streaming/attachments, non-personal workspaces, iPad, or lower iOS runtime.
- **State ownership conclusion**: Auth ownership is unchanged. `AuthTransientSession` remains short-lived transport only. `ProtocolReadProbe` remains diagnostic-only and persists no production conversation state. Production conversation repository/selection/message-tree ownership should be established in `DEV-native-read-path`.
- **Important next-phase evidence**: The tested first detail payload was large (~13.15 MB / 2068 mapping nodes). `DEV-native-read-path` must treat large-conversation storage/rendering/performance as a real current input, but b7 does not prove which portion of the 13.57 s total belongs to transport vs parsing.
- **Rejected / do-not-repeat**: No speculative automatic retry/fallback/timer, UA spoofing, Cloudflare bypass, persistent copied credentials, browser-only headers without evidence, payload/title/body logging, or production conversation models inside this diagnostic task.
- **Next exact action**: Update durable project docs and `BUILD_TEST_INDEX.md` with accepted b7 runtime facts, add the confirmed read-protocol decision, remove this completed checkpoint, mark PR #7 ready, merge if its head remains the expected conflict-free documentation-only completion head, then verify `main` and record the merge in durable docs as needed.
