# DEV-native-read-path

## Status

**Active**

- **Work ID**: `DEV-native-read-path`
- **Routing aliases / keywords**: `官方 App UI 主框架与原生会话读取 / 原生会话读取 / 官方 App UI / native read path`
- **Task**: Build the official-App-style native main shell and establish production conversation list/detail/message read ownership.
- **User intent / acceptance criteria**: Replace the diagnostic landing screen with the first usable native read experience: official-style sidebar/main-chat shell, authenticated native conversation list, selected-conversation identity owned outside UI text, conversation-detail loading, and readable current-branch user/assistant messages. Keep b7 auth/protocol evidence boundaries and do not add speculative retry/fallback behavior.
- **Baseline / base branch**: `main` remains `2ad48e5b1544dee4611edfaea85583e0a3aa81a8`; accepted runtime baselines remain `DEV-auth-bootstrap-0.1.0-b6` and `DEV-protocol-read-0.1.0-b7`; deployment target iOS 14.0; target runtime iPhone / iOS 17.0.
- **Working branch / PR / current head**: `dev/native-read-path-20260826`; draft PR #9; latest product/config head for b9 is `d9c9b4da8bdecd2d6c097d4db2f3789300fc99c7` before this docs-only checkpoint commit.
- **Candidate identity**: **Current** `DEV-native-read-path-0.1.0-b9`, version/build `0.1.0 (9)`, artifact name `ChatGPTClient-0.1.0-b9-dev-native-read-path.ipa`. b9 was allocated only after the b8 runtime failure and after re-checking `BUILD_TEST_INDEX.md` + all Active checkpoints; no competing Active task/candidate exists. Historical b8 remains reserved evidence and is not reused.
- **State owner / shared dependencies**: `ConversationRepository` owns production conversation summaries, selected conversation identity, loaded detail and current visible branch messages. It holds only an in-memory `AuthTransientSession`; `AuthSessionStore` remains auth/account-context owner and default WebKit data store remains persistent auth-secret authority. `ProtocolReadProbe` remains diagnostic-only.
- **Frozen / do-not-touch**: No Frozen modules. Stable auth/account and protocol-read diagnostic contracts remain unchanged. No automatic retry, new headers, alternate endpoints, shadow WebView or persistent copied credentials were added.
- **Parallel conflicts checked against**: `docs/project/current/dev/` contains only this Active feature checkpoint plus the README template. PR #9 and branch match the checkpoint. `main` has not advanced from the recorded base. b9/build 9 is unique.
- **Completed through b8**: Production repository/state owner; list GET; selected identity ownership; detail GET identity check; current-branch extraction `current_node -> parent`; user/assistant text extraction; table-view virtualization; UIKit split sidebar/detail shell; settings/login/list reload entry points; b8 CI/artifact; first b8 real-device diagnostics review.
- **b8 runtime evidence**: Exact runtime metadata b8/build 8/source `e312acc3dd17`, Release, iPhone/iOS 17.0. Native shell launched. Initial list attempts had 0/0 WebKit cookies and account-context `missing_required_session_fields`; after explicit login/account verification, Plus/personal auth succeeded and production list GET repeatedly returned HTTP 200 with 28 items / total 29. One selected detail GET took `30935.12 ms` and returned HTTP 500 at `stage=response`; no payload parse, identity check or rendering occurred. b8 is runtime-tested but failed acceptance and is not Stable/Frozen.
- **b9 code change**: Commit `7ab93653ab033be23008b4a0746c21609c43b6f4` adds privacy-safe selected-conversation diagnostics and terminal manual detail reload only. `conversation.selected`, `detailLoad`, `detail.request/response`, network failure/discard and manual reload now carry a 12-hex SHA-256-derived `conversationHash` plus 1-based `listPosition` when the selected item is in the loaded list. No raw conversation ID or body is logged. Terminal detail failure shows an explicit `重新加载` button; pressing it performs exactly one user-triggered GET for the authoritative selected conversation and logs `conversation.detailReload.requested`.
- **b9 build/config change**: Xcode build number/candidate, package default candidate and Actions artifact label now use `DEV-native-read-path-0.1.0-b9`. Authoritative CI run is the push run for full head `d9c9b4da8bdecd2d6c097d4db2f3789300fc99c7`, run `32978476582`; it was in progress when this checkpoint was written. Earlier intermediate push runs are not authoritative for b9.
- **Validation state**: b8 = Code written + CI passed + Artifact produced + Runtime/manual/real-device tested (partial/failing acceptance). b9 = **Code written + candidate allocated; authoritative CI currently in progress**. No b9 Artifact or runtime result is claimed yet.
- **Pending**: Finish b9 CI; if successful, record exact artifact ID / IPA hash / source identity and hand off only that exact b9 artifact. On device, select at least two conversations (prefer one ordinary/small and the previously failing/large candidate if recognizable), record whether hashes/positions differ, whether detail returns 200 vs 500, and use terminal `重新加载` once on a failed detail if needed. This will distinguish conversation-specific failure from systematic detail failure without changing protocol behavior.
- **Next exact action**: Check GitHub Actions run `32978476582` for full b9 head `d9c9b4da8bdecd2d6c097d4db2f3789300fc99c7`; if it fails, inspect exact compiler/job evidence and make only the smallest evidence-backed correction. If it passes, record artifact identity/hash in checkpoint + `BUILD_TEST_INDEX.md` and provide the exact b9 IPA for the described multi-conversation real-device test.
- **Rejected / do-not-repeat**: No automatic session/detail retry; no UA spoof/Cloudflare bypass; no speculative account/browser headers; no fallback conversation endpoints; no UI-title identity; no naive all-mapping view materialization; no reuse of `ProtocolReadProbe` as production repository; no hidden/shadow WebView.
- **Open questions / risks**: Whether detail HTTP 500 is conversation-specific remains Unknown / Unverified. Large mapping JSON parse-time/memory remains unverified because b8 failed before payload parsing. Persistent auth across install/update remains unverified because the tested b8 launch began with zero WebKit cookies. b9 is diagnostic discrimination, not a claim that the server-side 500 has been fixed.
