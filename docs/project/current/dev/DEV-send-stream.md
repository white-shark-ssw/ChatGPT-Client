# DEV-send-stream

## Status

**Active — b65 focused probe Runtime passed. TD-029 is the current production Send architecture. Future `DEV-composer-parity` owns final Native Composer UI. Current Work is now at b66 existing-conversation production bridge assembly: Web Rule Lab + covered official-Web protected-Send executor + Repository-owned live response + validation-only trigger. Stable/Frozen Send remains No. PR #29 remains open / mergeable / unmerged.**

- Work ID: `DEV-send-stream`
- Branch: `dev/send-stream-20260829`
- PR: #29 — open / mergeable / not merged
- Formal branch head immediately before this checkpoint refresh: `567be02df6f7832dbc41d6c39cacd9aba5a862aa`
- Formal branch tree at that head: `ab7a8329a30972e02beac52fa6291f447873ee33`
- Current actual `main`: `d323b9eed2dda75b9986fc06e14014d3e9b365fb`
- `main` advance inspected in this session remains docs/rules planning only; no current Root/Settings/pbx/workflow product overlap observed. Re-check before final CI/merge.
- `DEV-composer-parity` is a future serialized Work, not a parallel Active checkpoint/branch/Candidate.
- Stable native predecessor: b38.
- Latest Runtime-tested Candidate: `DEV-send-stream-0.1.0-b65`.
- b39-b65 emitted identities are permanently reserved.
- b66 is now the next uniquely allocated identity for the coherent TD-029 production/Web-Lab slice, but **no formal-branch b66 commit, CI or Artifact has been emitted yet at this checkpoint**.

## Exact b65 Runtime — accepted predecessor

Exact build65/source `44138db766d0` passed focused iPhone/iOS17 Runtime:

`verified composer -> one page-owned protected Send -> sendObserved -> HTTP200 text/event-stream -> thinking/reasoning/tool/final -> [DONE] terminal`.

Terminal evidence included reasoning `14/295`, final `71/2827`, exact parent association `10/10/0/0` matched/unmatched/missing, tool presentation/completion `10/10`, structured nested `工具输入` / `工具输出`, and no user-observed truncation. Minor spacing/slash-escape polish is non-blocking.

Exact b65 package authority:

- Candidate `DEV-send-stream-0.1.0-b65`, `0.1.0 (65)`;
- source `44138db766d00e62cfda7f20182f6d20f1ec3352`;
- Push Artifact `9736876465`;
- IPA SHA `e6a01b2eafd361b9df2567b002f9e8aa56b57dcee219c7999c65767b91138d16`.

## Current production authority — TD-029

Production conceptual path:

`Native send action -> ConversationRepository response operation -> covered official Web verified composer/page-owned protected Send -> same-response SSE -> Repository incremental response state -> Native consumers`.

Hard rules:

1. official page owns browser challenge + exactly one protected Send;
2. Native never solves/replays/persists challenge material;
3. covered Web owns no conversation/message/response/draft state;
4. `ConversationRepository` is sole production conversation/response owner;
5. one user Send -> exactly one protected Send; Sync/Reload never resend;
6. full Web conversation UI remains rejected;
7. current selectors/SSE semantics live in `WEB_SEND_ADAPTER.md`;
8. future Web changes are probed through Web Rule Lab before production rule changes.

## Composer parity ownership carve-out

Current `DEV-send-stream` owns protected Send/response/Stop lifecycle APIs, existing/new conversation identity semantics, reasoning/final/tool event semantics, Web Rule Lab, multi-conversation/follow-tail acceptance and only a **minimal validation text trigger**.

Future serialized `DEV-composer-parity` owns final bottom Composer hierarchy, dynamic multiline auto-growth, full-screen editor, keyboard/layout animation, per-conversation drafts, attachment staging/preview, mode/reasoning-effort controls and final Send/Stop presentation.

Do not add final Composer behavior in this Work.

## b66 coherent product slice — prepared

The first production slice intentionally does **not** yet claim new-chat identity, server Stop, hidden-active memory-warning resident protection or final follow-tail acceptance. It exists to prove the production owner/transport boundary on an authoritative existing conversation.

Behavior assembled:

- process-resident covered official-Web executor uses `WKWebsiteDataStore.default()` and exact-targets `/c/<conversationID>`;
- accepted b65 composer selectors only: `#prompt-textarea` or explicit `[contenteditable="true"][role="textbox"]`;
- page owns one protected `/backend-api/f/conversation` Send;
- same-response SSE parser keeps exact top-level `o/p/v`, contextual `v`, exact `title_generation` continuation, nested text-patch scrubbing, service-marked reasoning preamble/active/end, exact-parent tool association and real `[DONE]` terminal only;
- consumed visible text patches are removed before the stream is returned to covered Web, preserving the reduced hidden-render dependency;
- Repository instance owns per-conversation generation + `preparing/thinking/reasoning/final/completed/failed` + tool lifecycle snapshot;
- Root is orchestration/UI consumer only; no second prompt/answer/reasoning store;
- terminal invokes existing `syncLatestMessages`; live snapshot clears only if authoritative visible message count actually grows beyond pre-Send baseline;
- selected active conversation disables unsafe Sync/Reload rather than guessing reconciliation;
- Web Rule Lab is visible, explicit-execute, same persistent WebKit store, temporary script/result only, copy/share allowed, no persisted body/log body.

Prepared product blobs:

- `ChatGPTClient/RootViewController.swift` -> `b613971ddacae73391fe5b8c92a2e9113af76e2e`;
- `ChatGPTClient/SettingsViewController.swift` -> `ce4d360db9ddab4d8fc739c8ff1815748cfbc644`;
- b66 `ChatGPTClient.xcodeproj/project.pbxproj` -> `7eb1ab1eadd3a42dacc1123fd2deabdb297a4b1b`;
- b66 `.github/workflows/ios-foundation.yml` -> `7b2cbfb96fdf2afed8a2b340d3b37afa92df3664`.

Product-only detached predecessor `536fcbfcd1fbf9ed58b96cc4d1814ca4b1ec9ff5` was audited from the earlier recovery head and changed only Root/Settings. Tooling assembly branch `tmp-b66-assembly-20260831` added only pbx/workflow identity; its final commit `bae758aa32742444facd7e10e476d686a27c3abf` compares against recovery head with exactly four expected files. This temp branch is **tooling-only**, has no Work/Candidate/Artifact authority, and must never be routed as development state.

## Tooling-only empty-file incident history — no product/Candidate effect

Connector/action-selection mistakes created eleven temporary **empty** root files on the formal branch. Every one was immediately deleted; the tree returned to the intended product/docs tree after each pair. None contained source/config/workflow data and none emitted Candidate/CI/Artifact identity. Never replay these chains.

Earlier four pairs already recorded:

1. `tmp_should_not_create`: `53fc4245...` -> removed `37175cb7...`.
2. `THIS_CALL_MUST_NOT_HAPPEN`: `ef95ae34...` -> removed `a75df694...`.
3. `SHOULD_NEVER_BE_CREATED_AGAIN`: `ebef531c...` -> removed `a10b6829...`.
4. `NO_MORE_CONTENTS_WRITES`: `306bc3d3...` -> removed `b54c3073...`.

Additional seven pairs from the same tooling failure:

5. `DO_NOT_CREATE_THIS_FILE`: `3368b92b904ade4659b3a58079478bfe193c1800` -> removed `c394f48acbfd89ddaebd45591f4c8fe3e874e5f0`.
6. `THIS_SHOULD_NOT_EXIST`: `d45a5557bf0180e2b2811cb80d78bb2ff75edca3` -> removed `980ac876004ade270fc124123af0c2ec824d6cce`.
7. `LAST_ACCIDENTAL_EMPTY_FILE`: `067d67aa9e05fb53c97cf3a3433d63f310afe5a3` -> removed `e8042bd1e40c3980d9c2fc612b6f22e54269888c`.
8. `STOP`: `0cdeab4e0bbe888430b987bee54c139e4032f72e` -> removed `a2ae03208632a515fd8bd7fc073fccdb137ef014`.
9. `TOOL_SELECTION_FAILURE`: `388435b00bf6c6d78628658f39b10f1ede14af95` -> removed `07bbb4e66dd2ad2a8b90ab9856148c5f4a28c8a1`.
10. `SCHEMA_MISMATCH`: `800f0580c31bc1cffc4d884e6042b1435bfeb610` -> removed `c407c9e242734f76cc515fef22cd694f9f40a072`.
11. `WHY_IS_THIS_HAPPENING`: `1a547d30bce070f7ba27ee547de292dc90bd2035` -> removed `567be02df6f7832dbc41d6c39cacd9aba5a862aa`.

Because the create/delete history is tooling-only, product authority is determined by tree/content identity, Candidate package identity and Runtime evidence—not by these empty commits.

## Batch recovery point — exact next writes

After this checkpoint commit lands:

1. fetch the new formal head/tree;
2. build a Git tree from that head replacing exactly the four b66 blobs listed above;
3. create one single-parent product/config commit with the new formal checkpoint head as parent;
4. compare-audit: expected four files only relative to the new checkpoint head;
5. re-check actual `main` and PR/head; if unchanged/non-overlapping, fast-forward `dev/send-stream-20260829` once to that exact b66 commit;
6. then continue through Push + PR CI, Artifact download, independent ZIP/IPA/package identity verification;
7. update durable docs/PR/checkpoint and stop only at exact iPhone/iOS17 Runtime gate.

Do not touch final Composer, attachments, b38 conversation geometry, auth owner/default WebKit store, or b39-b65 identities.
