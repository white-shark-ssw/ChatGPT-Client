# DEV-protocol-read

## Status

**Active**

- **Work ID**: `DEV-protocol-read`
- **Routing aliases / keywords**: `协议读取 / 会话列表 / 会话详情 / conversation list / conversation detail / protocol read`
- **Task**: Establish current ChatGPT conversation-list and conversation-detail protocol evidence using the merged, runtime-tested authentication/account-context baseline before production conversation models or native read UI are implemented.
- **User intent / acceptance criteria**: Build the smallest authenticated diagnostic harness that can load the current conversation list and one conversation detail on the intended iPhone while recording privacy-safe structural/status/timing evidence only. Do not log message bodies or auth secrets and do not infer streaming/send behavior from read-path evidence.
- **Baseline / resume identity**: `main` remains `5547b32ed8c794052eb80bb751abf33121b62926`. Auth PR #6 is merged at `78f42a06e6254088e3b495cb4529e549a1d4717f`. Accepted auth runtime baseline is `DEV-auth-bootstrap-0.1.0-b6`, product source `19c0cd22923d8c6f4c96e676258b31814d02a942`, iPhone / iOS 17.0.
- **Working branch / PR / head**: `dev/protocol-read-20260826`; draft PR #7 `DEV-protocol-read: conversation list and detail evidence`; verified PR head before this checkpoint sync was `75db4dd54d21834ec4a8cbfa11c53a1c45ed2651`; PR is open/draft/mergeable. Branch started exactly from current `main` `5547b32ed8c794052eb80bb751abf33121b62926` and `main` has not advanced.
- **Candidate identity**: **Reserved `DEV-protocol-read-0.1.0-b7`, version `0.1.0 (7)`**. Xcode target settings, `scripts/build_ipa.sh`, workflow artifact name and `BUILD_TEST_INDEX.md` all agree on b7. Intended IPA is `ChatGPTClient-0.1.0-b7-dev-protocol-read.ipa`. No b7 artifact has been produced yet.
- **Checkpoint reconciliation**: An earlier checkpoint revision still said “candidate not allocated”, but current branch source, build index, Xcode settings, CI workflow and prior explicit b7 continuation all agree that b7 is the active reserved identity. This checkpoint records the current GitHub/source truth; task/branch/PR identity is unchanged.
- **Accepted auth dependency**: Default persistent `WKWebsiteDataStore` remains the sole persistent auth-secret authority. `AuthSessionStore` owns accepted in-memory account context. Copied cookies and `/api/auth/session` bearer remain transient only. No speculative automatic retry exists.
- **Current external protocol evidence — not runtime acceptance**: Recent independent public references agree on Bearer-authenticated `GET /backend-api/conversations?offset=0&limit=28&order=updated` and `GET /backend-api/conversation/{conversation_id}`, with detail `mapping`, `current_node`, parent/children and message role/content-type structure. These sources are evidence candidates only; this app still requires iPhone runtime proof.
- **Minimal first-request decision**: First probe uses only the accepted transient Bearer + copied ephemeral WebKit cookies for the accepted Plus/personal account. Do not add `chatgpt-account-id`, browser-only headers, fallback endpoints, retries, UA spoofing or Cloudflare bypass without concrete device evidence.
- **Code written**: `AuthSessionStore` can return a private `AuthTransientSession` after the accepted account probe verifies; the bearer is injected internally and never exposed or persisted. `ProtocolReadProbe` performs one list GET, selects the first returned conversation ID only in memory, performs one detail GET, and records safe counts/structure only. `AuthWebViewController` has auth-only and protocol-read modes. `RootViewController` exposes `开始会话列表与详情验证`. Xcode includes `ProtocolReadProbe.swift`.
- **Protocol diagnostics boundary**: Allowed: HTTP status, duration, response byte count, list item/pagination counts, mapping/message/root/branch/max-child counts, role counts, content-type count, current-node presence/mapping and conversation identity presence/match. Excluded: titles, message bodies/parts, payload dumps, raw conversation/message IDs, Cookie values, bearer/token values and Authorization headers.
- **State ownership**: Authentication/account owners remain unchanged. `AuthTransientSession` is short-lived transport capability only. `ProtocolReadProbe` is diagnostic-only and persists no conversation payload/model/repository. Production conversation ownership is deferred to `DEV-native-read-path`.
- **Files/modules changed**: `ChatGPTClient/Authentication/AuthSessionStore.swift`, `ChatGPTClient/Authentication/AuthWebViewController.swift`, `ChatGPTClient/RootViewController.swift`, new `ChatGPTClient/Protocol/ProtocolReadProbe.swift`, `ChatGPTClient.xcodeproj/project.pbxproj`, build/CI identity files and project docs. Diagnostics implementation itself is reused unchanged.
- **Stable/Frozen impact**: Auth/account context, shell, diagnostics and packaging are Stable, not Frozen. Changes are limited to the explicit protocol diagnostic path and candidate identity. No Frozen modules recorded.
- **Parallel/conflict guard**: No other Active development checkpoint exists. No duplicate branch or candidate identity exists. Core roadmap remains serialized `foundation -> auth -> protocol read -> native read -> send/stream`.
- **CI evidence**: First b7 CI run `32938007843` failed in Release Swift compilation because the compiler could not type-check the large `ProtocolReadProbe` list/detail closure in reasonable time. Code was split into dedicated handlers. Second b7 CI run `32938132841` at PR merge source `6c5da85989d4` / branch head `75db4dd54d...` failed at `ProtocolReadProbe.swift:106` for the same compiler class on the large detail diagnostics dictionary literal. No network/protocol request ran; this is compile-only evidence.
- **Validation state**: **Code written; CI currently failed on a concrete Swift type-check issue.** No b7 artifact, no real-device list/detail request, and no runtime protocol acceptance yet.
- **Next exact action**: Split only the large detail diagnostics dictionary literal at `ProtocolReadProbe.swift:106` into explicitly typed incremental assignments, preserving the exact fields/behavior. Push that minimal compile fix, inspect the new CI result, then if CI passes verify b7 artifact identity/checksum before device testing.
- **Rejected / do-not-repeat**: Guessing endpoints/shapes from old history; adding personal-account headers merely because Teams may need them; logging payload bodies/titles/raw IDs; persistent copied credentials; speculative retry/fallback/timer/UA spoof/Cloudflare bypass; production conversation models before protocol evidence; treating CI or external references as runtime proof.
- **Open risks / questions**: Device-required headers beyond Bearer/cookies remain unverified. List/detail responses may differ from external references. Native requests may encounter route-specific challenge behavior. Non-personal workspace behavior, lower iOS versions and iPad remain unverified.
