# DEV-protocol-read

## Status

**Active — b7 ready for device protocol validation**

- **Work ID**: `DEV-protocol-read`
- **Routing aliases / keywords**: `协议读取 / 会话列表 / 会话详情 / conversation list / conversation detail / protocol read`
- **Task**: Establish current ChatGPT conversation-list and conversation-detail protocol evidence using the merged, runtime-tested authentication/account-context baseline before production conversation models or native read UI are implemented.
- **User intent / acceptance criteria**: Build the smallest authenticated diagnostic harness that can load the current conversation list and one conversation detail on the intended iPhone while recording privacy-safe structural/status/timing evidence only. Do not log message bodies or auth secrets and do not infer streaming/send behavior from read-path evidence.
- **Baseline / resume identity**: `main` remains `5547b32ed8c794052eb80bb751abf33121b62926`. Auth PR #6 is merged at `78f42a06e6254088e3b495cb4529e549a1d4717f`. Accepted auth runtime baseline is `DEV-auth-bootstrap-0.1.0-b6`, product source `19c0cd22923d8c6f4c96e676258b31814d02a942`, iPhone / iOS 17.0.
- **Working branch / PR / head**: `dev/protocol-read-20260826`; draft PR #7 `DEV-protocol-read: conversation list and detail evidence`; branch head before this checkpoint update was `867d02b8016b0e1e0d60c4ae3805e8c49d015c36`, PR open/draft/mergeable. Exact b7 product/workflow source is `44a137b973e29e2a313e9114fdacb7727dccefb9`. Compare `44a137... -> 867d02...` shows only six `docs/project/**` files changed, so post-build documentation commits do not invalidate the b7 product artifact.
- **Candidate identity**: **`DEV-protocol-read-0.1.0-b7`, version `0.1.0 (7)`**. Xcode target settings, `scripts/build_ipa.sh`, CI workflow and `BUILD_TEST_INDEX.md` agree on b7. IPA `ChatGPTClient-0.1.0-b7-dev-protocol-read.ipa`.
- **Authoritative CI/artifact evidence**: Exact-branch push run **`32938912018`** checked out source `44a137b973e29e2a313e9114fdacb7727dccefb9`, used Xcode 16.4 / iPhoneOS 18.5, embedded `SOURCE_COMMIT=44a137b973e2` and `DIAGNOSTICS_CANDIDATE=DEV-protocol-read-0.1.0-b7`, compiled `arm64-apple-ios14.0`, and ended `BUILD SUCCEEDED`. Artifact ID **`9595827498`**, artifact `ChatGPTClient-DEV-protocol-read-0.1.0-b7`, IPA SHA-256 **`64b0cc055bc9da27bc887698ba18ae5cb2cc0fdb9f15a3a59eb09e55c5fcb4ae`**, artifact ZIP digest **`sha256:c1d851dc949a43587f94fffd34b35c233ff5f35a2c8eef3399d2e722a9f7833f`**. Downloaded ZIP and IPA were independently unpacked/hashed and match GitHub + sidecar exactly.
- **Supplemental PR CI**: PR run `32938915653` also passed against the generated merge ref, but it embedded merge-source `889de79...`; it is supplemental merge-compatibility evidence only. The exact branch push run `32938912018` is the authoritative device candidate identity.
- **Compile history**: Run `32938007843` failed only because Swift could not type-check a large list/detail closure; run `32938132841` then exposed the same compiler class on one large detail diagnostics dictionary literal. Commit `44a137...` split these expressions for compiler tractability without changing protocol/auth behavior. Neither failed run performed runtime protocol requests.
- **Accepted auth dependency**: Default persistent `WKWebsiteDataStore` remains the sole persistent auth-secret authority. `AuthSessionStore` owns accepted in-memory account context. Copied cookies and `/api/auth/session` bearer remain transient only. No speculative automatic retry exists.
- **Minimal first-request decision**: First probe uses only accepted transient Bearer + copied ephemeral WebKit cookies for the accepted Plus/personal account. Do not add `chatgpt-account-id`, browser-only headers, fallback endpoints, retries, UA spoofing or Cloudflare bypass without concrete device evidence.
- **Code written**: `AuthSessionStore` can return a private short-lived `AuthTransientSession` after accepted account verification; bearer injection is internal and never exposed/persisted. `ProtocolReadProbe` performs one list GET, selects the first returned conversation ID only in memory, performs one detail GET, then discards payload data after safe structural inspection. `AuthWebViewController` has auth-only and protocol-read modes. `RootViewController` exposes `开始会话列表与详情验证`.
- **Protocol diagnostics boundary**: Allowed: HTTP status, duration, response byte count, list item/pagination counts, mapping/message/root/branch/max-child counts, role counts, content-type count, current-node presence/mapping and conversation identity presence/match. Excluded: titles, message bodies/parts, payload dumps, raw conversation/message IDs, Cookie values, bearer/token values and Authorization headers.
- **State ownership**: Authentication/account owners remain unchanged. `AuthTransientSession` is short-lived transport capability only. `ProtocolReadProbe` is diagnostic-only and persists no production conversation payload/model/repository. Production conversation ownership remains deferred to `DEV-native-read-path`.
- **Stable/Frozen impact**: Auth/account context, shell, diagnostics and packaging remain Stable, not Frozen. Protocol-read diagnostic transport is Candidate, not Stable. No Frozen modules recorded.
- **Parallel/conflict guard**: No other Active development checkpoint exists. No duplicate branch or candidate identity exists. `main` has not advanced from the task base. Core roadmap remains serialized `foundation -> auth -> protocol read -> native read -> send/stream`.
- **Validation state**: **Code written + CI passed + Artifact produced. Runtime/manual/real-device protocol validation is pending.** Do not describe list/detail endpoints or mapping semantics as accepted yet.
- **Next exact action**: Install authoritative b7 IPA, clear accumulated diagnostics in Settings, run `开始会话列表与详情验证`, then export diagnostics immediately. Success target is the protocol-read success title after account verification; if account/session is challenge-sensitive, preserve the exact failure and only use an explicit user-triggered restart. If list/detail fails, retain exact stage/status and change only what runtime evidence justifies.
- **Rejected / do-not-repeat**: Guessing endpoints/shapes from old history; adding personal-account headers merely because Teams may need them; logging payload bodies/titles/raw IDs; persistent copied credentials; speculative retry/fallback/timer/UA spoof/Cloudflare bypass; production conversation models before protocol evidence; treating CI or external references as runtime proof.
- **Open risks / questions**: Device-required headers beyond Bearer/cookies remain unverified. List/detail response may differ from external references. Native requests may encounter route-specific challenge behavior. Non-personal workspace behavior, lower iOS versions and iPad remain unverified.
