# DEV-native-read-path

## Status

**Active**

- **Work ID**: `DEV-native-read-path`
- **Routing aliases / keywords**: `官方 App UI 主框架与原生会话读取 / 原生会话读取 / 官方 App UI / native read path`
- **Task**: Build the official-App-style native main shell and establish production conversation list/detail/message read ownership.
- **User intent / acceptance criteria**: Replace the diagnostic landing screen with the first usable native read experience: official-style sidebar/main-chat shell, authenticated native conversation list, selected-conversation identity owned outside UI text, conversation-detail loading, and readable current-branch user/assistant messages. Keep b7 auth/protocol evidence boundaries and do not add speculative retry/fallback behavior.
- **Baseline**: `main` at `2ad48e5b1544dee4611edfaea85583e0a3aa81a8`; accepted runtime baselines `DEV-auth-bootstrap-0.1.0-b6` and `DEV-protocol-read-0.1.0-b7`; iPhone / iOS 17.0 runtime evidence; deployment target iOS 14.0.
- **Working branch / PR / tested product source**: `dev/native-read-path-20260826`; draft PR #9; tested b8 product source `e312acc3dd17cdcdb01746bb76f70556510a0304`.
- **Candidate identity**: `DEV-native-read-path-0.1.0-b8`; version/build `0.1.0 (8)`; artifact `ChatGPTClient-0.1.0-b8-dev-native-read-path.ipa`; run `32976656499`; artifact ID `9609766005`; IPA SHA-256 `50bfb7453443c41de8661c1ffc3e2a7076fd939fe62453aceacb599371862acd`.
- **State owner / shared dependencies**: `ConversationRepository` owns production conversation summaries, selected conversation identity, loaded detail and current visible branch messages. It holds only an in-memory `AuthTransientSession`; `AuthSessionStore` remains auth/account-context owner and default WebKit data store remains persistent auth-secret authority. `ProtocolReadProbe` remains diagnostic-only.
- **Frozen / do-not-touch**: No Frozen modules. Stable auth/account and protocol-read diagnostic contracts remain unchanged. No automatic retry, new headers, alternate endpoints or persistent copied credentials were added.
- **Parallel conflicts checked against**: Task creation and b8 allocation scans found no competing Active checkpoint/candidate. b8 remains reserved to this task.
- **Completed**: Production repository/state owner; list GET; selected identity ownership; detail GET identity check; current-branch extraction `current_node -> parent`; user/assistant text extraction; table-view virtualization; UIKit split sidebar/detail shell; settings/login/error/reload entry points; Xcode membership; unique b8 artifact; CI correction and success; draft PR #9; first real-device b8 run and diagnostics review.
- **Validation state**: **Code written + CI passed + Artifact produced + Runtime/manual/real-device tested (partial / failing acceptance).** Exact runtime metadata: b8/build 8/source `e312acc3dd17`, Release, iPhone, iOS 17.0. App launch and native shell succeeded. Initial native list attempts had `0/0` WebKit cookies and account-context `missing_required_session_fields`; after the user explicitly opened the existing login/account verification surface, WebKit reported authenticated with 46/27 then 48/29 cookies, session/accounts returned HTTP 200 and plus/personal context verified. Production list GET then succeeded repeatedly with 28 items / total 29 and HTTP 200. One user-selected conversation detail GET ran for `30935.12 ms` and returned HTTP 500 at `stage=response`; no `detail.response`, JSON parse, identity-check or render stage was reached. Therefore production list/auth reuse after explicit login is runtime-proven, but selected-conversation detail/message rendering is **not accepted** and b8 is not Stable/Frozen.
- **Evidence interpretation**: The observed detail failure is currently a server HTTP 500 response, not evidence of a local mapping parser/render failure. The exact selected conversation ID is intentionally not logged, so this single export cannot prove whether the 500 is conversation-specific or reproducible across arbitrary conversations. The initial 0-cookie condition also shows b8 did not retain usable WebKit auth data across this install/run state; the log alone does not prove whether TrollStore install/replacement cleared the app container or whether another persistence issue exists.
- **Pending**: Reproduce detail loading against at least one small/ordinary conversation and the previously evidenced large conversation, while preserving no-auto-retry policy. Improve diagnostics only as needed to distinguish request identity safely (hashed ID / list position), response timing and repeated manual attempts without logging raw conversation IDs or content. Determine whether detail HTTP 500 is conversation-specific or systematic before changing transport/protocol code.
- **Next exact action**: Make the smallest diagnostic improvement that records a privacy-safe hash of selected conversation identity and list index on `conversation.selected` / `detailLoad`, plus an explicit manual `重新加载` action on terminal detail error if not already reachable; produce a new unique candidate only after code changes. Do not add automatic retry or alternate endpoint/header behavior from this single HTTP 500 observation.
- **Rejected / do-not-repeat**: No automatic session retry; no UA spoof/Cloudflare bypass; no speculative account/browser headers; no fallback conversation endpoints; no UI-title identity; no naive all-mapping view materialization; no reuse of `ProtocolReadProbe` as production repository; no hidden/shadow WebView for native reads.
- **Open questions / risks**: Whether detail HTTP 500 is conversation-specific is Unknown / Unverified. Large mapping JSON parse-time/memory remains unverified because this b8 detail request failed before payload parsing. Compact-width shell itself launched, but detailed UI acceptance remains incomplete. Persistent auth across install/update also remains unverified because the tested b8 launch began with zero WebKit cookies.
