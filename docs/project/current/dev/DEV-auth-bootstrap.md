# DEV-auth-bootstrap

## Status

**Active**

- **Work ID**: `DEV-auth-bootstrap`
- **Routing aliases / keywords**: `网页登录 / Google 登录 / Google 登录验证 / 登录验证 / auth bootstrap`
- **Task**: Establish the current ChatGPT login/session bootstrap on real device, including WebKit persistence, native-session consumption evidence, and the account/workspace context required before native private-protocol read work.
- **User intent / acceptance criteria**: Provide a testable iOS login path, preserve privacy-safe diagnostics, prove session persistence and native consumption from current evidence, then establish one explicit account/workspace context owner before protocol-read work. Use the user-supplied app icon. Do not claim private conversation-protocol authentication until separately evidenced.
- **Baseline**: `main` base remains `836651a41e36feafcc2386939d70d673be6e3725`; accepted Stable foundation is `DEV-app-foundation-0.1.0-b1`, version `0.1.0 (1)`, TrollStore tested on iPhone / iOS 17.0.
- **Working branch / PR / head commit**: `dev/auth-bootstrap-20260826`; draft PR #6 `DEV-auth-bootstrap: web and Google login verification`; PR head verified before this runtime-evidence documentation cycle at `3b927cfc38fc696004adbf55905dc343316309a2`. b3 runtime-tested product source remains exactly `0fcf040012c0698d0e3ce1628fec9865237eba3b`; later branch commits are documentation-only unless separately stated.
- **Candidate identity**: b2 runtime evidence retained as `DEV-auth-bootstrap-0.1.0-b2`. b3 runtime evidence is `DEV-auth-bootstrap-0.1.0-b3`, version `0.1.0 (3)`, authoritative IPA `ChatGPTClient-0.1.0-b3-dev-auth-bootstrap.ipa`, artifact `ChatGPTClient-DEV-auth-bootstrap-0.1.0-b3` from push run `32889095904` / artifact ID `9578766019` / IPA SHA-256 `b377d3f085d1877c16baf79d3969af21d5345517261b6eda87a7637aef292860`.
- **Evidence**: b2 established embedded Continue with Google plus force-close/relaunch WebKit persistence. b3 now has real-device acceptance on iPhone / iOS 17.0. The user supplied a screenshot showing title `网页登录成功 · 原生会话通过` and a diagnostic export identifying candidate `DEV-auth-bootstrap-0.1.0-b3`, build `3`, source `0fcf040012c0`, iPhone / iOS 17.0. The b3 trace records `session.webState=authenticated`, `nativeSessionProbe.webData itemCount=54 matchedItemCount=35`, then `session.nativeState=verified`; `nativeSessionProbe.end` completed `status=ok`, final host/destination `chatgpt.com` / `chatgpt`, HTTP `200`, duration `1203.68 ms`. No auth-secret values are present in the supplied probe events.
- **Files / modules in scope**: `ChatGPTClient/Authentication/AuthWebViewController.swift`; `ChatGPTClient/Authentication/AuthSessionStore.swift`; `ChatGPTClient/RootViewController.swift`; existing diagnostics authority; Xcode/build/artifact files where a new test candidate is required; checkpoint and durable project docs.
- **State owner / shared dependencies**: Default persistent `WKWebsiteDataStore` remains the only persistent auth-secret authority. `AuthSessionStore` owns safe web/native auth evidence state and the verified transient native bridge; it does not persist copied Cookie/token/header secrets. The tested native bridge transiently copies current ChatGPT/OpenAI WebKit cookies into an ephemeral `URLSession`.
- **Frozen / do-not-touch**: No modules are Frozen. Embedded web login/persistence and the b3 transient native bridge are accepted Stable for the tested scope/environment, not Frozen. Do not add browser fallback, persistent duplicate auth secrets, speculative retries/timers/watchdogs, or call this proof of private conversation APIs.
- **Parallel conflicts checked against**: Repository directory contains no other Active development checkpoint; branch/candidate are unique. PR #6 base remains `main` at `836651a41e36feafcc2386939d70d673be6e3725`. Core roadmap remains serialized foundation -> auth -> protocol read.
- **Completed**: Minimal embedded login; Continue with Google real-device success; privacy-safe auth diagnostics; app icon integration; WebKit persistence across force-close/relaunch; explicit `AuthSessionStore`; ephemeral native-cookie bridge; exact b3 CI/artifact identity; b3 real-device native-session verification with screenshot + diagnostics corroboration.
- **Validation state**: b2 is **Code written + CI passed + Artifact produced + Runtime/manual/real-device tested** for web login/persistence. b3 is **Code written + CI passed + Artifact produced + Runtime/manual/real-device tested** for transient WebKit-authenticated native `URLSession` acceptance of `/auth/login` on iPhone / iOS 17.0. No separate local Xcode/static checks are available. This does **not** prove account/workspace context or private conversation protocol yet.
- **Pending**: Establish current account/workspace context required by later native requests and assign one explicit owner. Before implementing a request, obtain current evidence for the relevant account/session endpoint/path, method, response shape and required request context; do not use historical endpoint memory as fact. After account context is runtime-evidenced, close the auth gate and only then open `DEV-protocol-read`.
- **Next exact action**: Research/establish a current account/session-context request from current evidence, then implement the smallest privacy-safe authenticated account/workspace probe on this same branch with a new unique test candidate only if runtime code changes are required.
- **Rejected / do-not-repeat**: Embedded Google necessarily requiring system-browser fallback; WebKit login failing to persist on the tested device; native `URLSession` being unable to consume the transient current WebKit session for the tested `/auth/login` route. Do not add speculative auth fallback/retry/timer chains. Do not persist copied Cookie values outside WebKit. Do not log passwords, OAuth codes, tokens, Cookie values or Authorization values.
- **Open questions / risks**: Current account/workspace context and any request headers/context required by later private conversation endpoints remain Unknown / Unverified. The b3 result proves only the tested auth route on one iPhone / iOS 17.0 environment; lower iOS versions, iPad, and private protocol behavior remain unverified.
