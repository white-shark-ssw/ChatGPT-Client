# DEV-auth-bootstrap

## Status

**Active**

- **Work ID**: `DEV-auth-bootstrap`
- **Routing aliases / keywords**: `网页登录 / Google 登录 / Google 登录验证 / 登录验证 / auth bootstrap`
- **Task**: Reproduce and verify the current ChatGPT web login path, specifically Continue with Google, on-device before native private-protocol work.
- **User intent / acceptance criteria**: Provide a testable iOS path that can open the current ChatGPT web login, complete Continue with Google, preserve privacy-safe auth/navigation diagnostics, verify session persistence, and establish evidence for how later native requests can consume authenticated state. Use the user-supplied 1024×1024 image as the app icon. Do not claim authenticated native protocol access until separately evidenced.
- **Baseline**: `main` remains at `836651a41e36feafcc2386939d70d673be6e3725`; accepted runtime foundation `DEV-app-foundation-0.1.0-b1`, version `0.1.0 (1)`, TrollStore tested on iPhone / iOS 17.0.
- **Working branch / PR / head commit**: `dev/auth-bootstrap-20260826`; draft PR #6 `DEV-auth-bootstrap: web and Google login verification`; runtime-tested product/build-input source `809fa03e673afded87cb47fb755c998ab1b58e12`; documentation head advances after runtime evidence recording.
- **Candidate identity**: Runtime-tested `DEV-auth-bootstrap-0.1.0-b2`, version `0.1.0 (2)`, IPA `ChatGPTClient-0.1.0-b2-dev-auth-bootstrap.ipa`, GitHub artifact `ChatGPTClient-DEV-auth-bootstrap-0.1.0-b2`, artifact ID `9577612707`. Next product change will require a new candidate/build identity.
- **Evidence**: GitHub Actions run `32886019320` passed on Xcode 16.4; b2 IPA SHA-256 `426c5f9b6b5e71a41c3ca571abdc73951835a55dc902691d75030a781ee61465`. User real-device testing on iPhone / iOS 17.0 completed Continue with Google successfully. The supplied b2 diagnostic export identifies version `0.1.0 (2)`, candidate `DEV-auth-bootstrap-0.1.0-b2`, source `809fa03e673a`, iOS `17.0`, and shows the initial Google flow reaching `accounts.google.com`, returning through `auth.openai.com`, then reaching `chatgpt.com` with HTTP 200. After force-close/relaunch, opening the same `/auth/login` entry immediately redirected to non-auth `chatgpt.com` with HTTP 200 and no Google navigation, matching the user's explicit report that the account remained signed in. No error/fault auth events and no password/token/Cookie/Authorization fields were observed in the exported diagnostic JSON.
- **Files / modules in scope**: `ChatGPTClient/Authentication/**`; `ChatGPTClient/RootViewController.swift`; existing diagnostics authority; `ChatGPTClient.xcodeproj/project.pbxproj`; `ChatGPTClient/Assets.xcassets/**`; `scripts/build_ipa.sh`; `scripts/reconstruct_app_icon.sh`; `scripts/assets/**`; `.github/workflows/ios-foundation.yml`; checkpoint and durable project docs.
- **State owner / shared dependencies**: Default persistent `WKWebsiteDataStore` is now runtime-evidenced as the current web-session persistence authority for b2. `AuthWebViewController` owns only login UI/navigation lifecycle. A native-consumable session state owner/bridge still must be established without creating a second persistent secret authority. Depends on Stable application shell and Stable diagnostics foundation.
- **Frozen / do-not-touch**: No modules are Frozen. Do not add a system-browser fallback while the tested embedded route works. Do not duplicate WebKit authentication secrets into a second persistent store. Do not assume native `URLSession` can consume the session until a direct native probe proves it.
- **Parallel conflicts checked against**: No other Active development checkpoint exists; dedicated branch/candidate remain unique; `main` has not advanced. Core roadmap serial dependency remains foundation -> auth -> protocol read.
- **Completed**: Minimal `WKWebView` login bootstrap; root login-verification entry; privacy-safe navigation diagnostics; b2 candidate and app icon integration; CI/artifact success; real-device Continue with Google success; force-close/relaunch test; persistent login state verified by user and corroborated by b2 diagnostics showing `/auth/login` redirect directly to authenticated ChatGPT after relaunch.
- **Validation state**: **Code written + CI passed + Artifact produced + Runtime/manual/real-device tested for embedded Google login and WebKit session persistence on iPhone / iOS 17.0**. Local Xcode/static checks were not available separately. Authentication/session is still **not Stable/Frozen** because native `URLSession` session consumption and current account/workspace context remain unverified.
- **Pending**: Establish one explicit authentication/session state owner that treats WebKit default data store as the persistent credential authority; perform the smallest native `URLSession` session-consumption probe by copying current WebKit cookies only into an ephemeral in-memory native session and requesting the already-verified `/auth/login` route, logging only cookie counts/final destination/status. If that succeeds, separately establish current account/workspace context before protocol-read implementation.
- **Next exact action**: Allocate a new b3 candidate, add the minimal WebKit-to-ephemeral-URLSession auth probe and explicit safe session-state owner, update build identity, run CI/artifact validation, then have the user install b3 while the WebKit login remains valid and report whether the native probe is accepted by ChatGPT.
- **Rejected / do-not-repeat**: The hypothesis that this tested embedded Google route requires a system-browser fallback is rejected. The hypothesis that default WebKit state might not survive relaunch is rejected for b2 on the tested device. Do not add speculative fallback/auth retry chains. Do not persist copied Cookie values outside the existing WebKit authority. Do not log passwords, OAuth codes, tokens, Cookie values or Authorization values. Do not call successful WebView login proof of authenticated native protocol access.
- **Open questions / risks**: Whether ChatGPT accepts the WebKit session when transiently copied into native `URLSession` is unknown; server behavior may depend on more than cookies. Current account/workspace context and any headers needed for later private protocol requests remain Unknown / Unverified and must be established from current evidence.
