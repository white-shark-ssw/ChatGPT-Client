# DEV-protocol-read

## Status

**Active**

- **Work ID**: `DEV-protocol-read`
- **Routing aliases / keywords**: `协议读取 / 会话列表 / 会话详情 / conversation list / conversation detail / protocol read`
- **Task**: Establish current ChatGPT conversation-list and conversation-detail protocol evidence using the merged, runtime-tested authentication/account-context baseline before production conversation models or native read UI are implemented.
- **User intent / acceptance criteria**: Start the next core development phase after successful auth. Build the smallest authenticated diagnostic harness that can load the current conversation list and one conversation detail on the intended iPhone, while recording privacy-safe structural/status/timing evidence only. Do not log message bodies or auth secrets and do not infer streaming/send behavior from read-path evidence.
- **Baseline / resume identity**: `main` exact start commit `5547b32ed8c794052eb80bb751abf33121b62926`. Auth PR #6 is merged; merge commit `78f42a06e6254088e3b495cb4529e549a1d4717f`. Accepted auth runtime baseline is `DEV-auth-bootstrap-0.1.0-b6`, product source `19c0cd22923d8c6f4c96e676258b31814d02a942`, iPhone / iOS 17.0.
- **Working branch / PR**: `dev/protocol-read-20260826`; draft PR #7 `DEV-protocol-read: conversation list and detail evidence`; branch started exactly from `main` `5547b32ed8c794052eb80bb751abf33121b62926`.
- **Candidate identity**: Not allocated yet. Current Xcode metadata still says build 6 / auth-b6 because no protocol artifact has been produced. Immediately before first runnable artifact, re-check `BUILD_TEST_INDEX.md`, all Active checkpoints, Xcode build/candidate settings, CI artifact names, branch/PR head and `main`; do not reuse b1-b6 identities.
- **Accepted auth dependency**: Default persistent `WKWebsiteDataStore` is the sole persistent auth-secret authority. `AuthSessionStore` owns accepted in-memory account context; copied cookies and `/api/auth/session` bearer are transient only. Current direct auth path can be challenge-sensitive; no speculative automatic retry is part of the architecture.
- **Current external protocol evidence — not runtime acceptance**: Two recent independent public implementations/references agree on the first read-path shape. `brianjlacy/export-chatgpt` commit `4cfc3f235ad41c864e7fe2369fb0037875537dbd` (README/spec updated 2026-04-06) documents Bearer auth, personal-account `chatgpt-account-id` as optional, `GET /backend-api/conversations?offset={offset}&limit=28&order=updated`, and `GET /backend-api/conversation/{conversation_id}` returning the mapping tree. `Octo-Lex/ChatGPT-Web2API` commit `497527dceabfa3f95961e23c291e618c5570f1ac`, captured 2026-06-02 and updated 2026-06-25, independently records the exact list request returning HTTP 200, detail request returning HTTP 200, `/api/auth/session` Bearer requirement, and detail fields `conversation_id`, `current_node`, `mapping`, node `parent`/`children`, message role/content-type structure. These are current external evidence only; this task still requires our iPhone runtime proof.
- **Minimal first-request decision**: Test the current Plus/personal account with only the accepted transient Bearer + copied ephemeral WebKit cookies. Do **not** add `chatgpt-account-id` on the first probe because current external evidence says it is optional for personal accounts and b6 selected `structure=personal`. Do not add `x-openai-target-path` or other captured browser headers without device evidence that they are required.
- **Code written so far**: `AuthSessionStore` now optionally returns an `AuthTransientSession` only after the already-accepted account probe verifies. It owns a new ephemeral `URLSession`, copies the same matched WebKit cookies, keeps the Bearer private, injects `Authorization` internally for requests, exposes no token accessor and has explicit invalidation. Existing auth-only mode requests no transient session, preserving the accepted path. `ProtocolReadProbe` performs one list GET (`offset=0`, `limit=28`, `order=updated`), selects only the first returned conversation ID in memory, performs one detail GET, records safe HTTP/byte/item/pagination/tree/role/content-type-count statistics and never logs title/message content or IDs. `AuthWebViewController` has separate auth-regression and protocol-read modes; protocol mode starts the read probe only after account verification. `RootViewController` exposes a separate `开始会话列表与详情验证` entry. Xcode now compiles the new `Protocol/ProtocolReadProbe.swift` source.
- **Protocol detail evidence collected by code, if runtime succeeds**: list HTTP status, bytes, item count, optional total/limit/offset; detail HTTP status, bytes, mapping/message/null/root/branch/max-child counts, user/assistant/system/tool/other role counts, content-type count, current-node presence/mapping and conversation identity presence/match. Full titles, message content/parts, metadata values, raw conversation/message IDs and auth secrets are excluded.
- **State ownership**: Authentication/account ownership remains unchanged. `AuthTransientSession` is a short-lived authenticated transport capability owned/created by `AuthSessionStore`; it is not a credential store. `ProtocolReadProbe` is a narrow diagnostic owner only and persists no conversation payload/model/repository. Production conversation identity/state ownership remains deferred to `DEV-native-read-path` after protocol evidence is accepted.
- **Files/modules changed**: `ChatGPTClient/Authentication/AuthSessionStore.swift`, `ChatGPTClient/Authentication/AuthWebViewController.swift`, `ChatGPTClient/RootViewController.swift`, new `ChatGPTClient/Protocol/ProtocolReadProbe.swift`, and `ChatGPTClient.xcodeproj/project.pbxproj`. Diagnostics implementation itself is unchanged and reused.
- **Stable/Frozen impact**: Auth/account context is Stable, not Frozen. Its only change is the optional transient authorized transport created after the existing verified account path; persistent ownership and accepted parsing are unchanged. Shell is Stable, not Frozen; its change is the explicit protocol diagnostic entry required by this task. Diagnostics remains unchanged. No Frozen modules recorded.
- **Parallel/conflict preflight**: After auth checkpoint completion/removal, `docs/project/current/dev/` on `main` contained only the template; no other Active development checkpoint existed. No branch/candidate conflict existed at creation. Core roadmap remains serialized `foundation -> auth -> protocol read -> native read -> send/stream`.
- **Validation state**: **Code written** for the minimal protocol-read diagnostic path. No compile/CI result yet, no protocol candidate/artifact allocated yet, and no real-device list/detail evidence yet. External endpoint/shape sources are not runtime proof for this app.
- **Next exact action**: Run the candidate-allocation guard now that the first runnable code path exists: re-check `main`, PR #7 head, all Active checkpoints, `BUILD_TEST_INDEX.md`, project build metadata, `scripts/build_ipa.sh` and CI workflow. Allocate the next unique protocol-read candidate only if conflict-free, update build/CI identity, run CI, fix only concrete compile/build failures, verify artifact identity/checksum, then provide the IPA for one clean real-device protocol-read run and exported diagnostics.
- **Rejected / do-not-repeat**: Guessing endpoints/shapes from old history; adding a personal-account account header merely because Teams needs one; captured browser-only headers without evidence; copying browser/session secrets into persistent native storage; logging payload bodies/titles/raw IDs; speculative retry/fallback/timer/UA spoof/Cloudflare bypass; creating production conversation models before list/detail shape evidence; treating auth or external public tools as our runtime protocol proof.
- **Open risks / questions**: Exact device-required headers beyond Bearer/cookies remain unverified. List/detail response may differ from external evidence. Direct native requests may encounter route-specific challenge behavior. Account-header requirements for non-personal workspaces remain unverified. Current workspace semantics beyond the selected personal account, lower iOS versions and iPad remain unverified.
