# DEV-native-read-path

## Status

**Active — completion/merge gate reached**

- **Work ID**: `DEV-native-read-path`
- **Routing aliases / keywords**: `官方 App UI 主框架与原生会话读取 / 原生会话读取 / 官方 App UI / native read path`
- **Task**: Build the official-App-style native main shell and establish production conversation list/detail/message read ownership.
- **User intent / acceptance criteria**: Replace the diagnostic landing screen with the first usable native read experience: official-style sidebar/main-chat shell, authenticated native conversation list, selected-conversation identity owned outside UI text, conversation-detail loading, and readable current-branch user/assistant messages. Keep b7 auth/protocol evidence boundaries and do not add speculative retry/fallback behavior.
- **Baseline / base branch**: `main` remains `2ad48e5b1544dee4611edfaea85583e0a3aa81a8`; accepted runtime baselines before this task were `DEV-auth-bootstrap-0.1.0-b6` and `DEV-protocol-read-0.1.0-b7`; deployment target iOS 14.0; target runtime iPhone / iOS 17.0.
- **Working branch / PR / tested product source**: `dev/native-read-path-20260826`; draft PR #9; authoritative b9 product/config source `d9c9b4da8bdecd2d6c097d4db2f3789300fc99c7`. Later branch-head commits are documentation-only evidence updates and do not change the built b9 product.
- **Candidate identity**: `DEV-native-read-path-0.1.0-b9`, version/build `0.1.0 (9)`, artifact `ChatGPTClient-0.1.0-b9-dev-native-read-path.ipa`; run `32978476582`; artifact ID `9610449216`; IPA SHA-256 `16168a9db6f03e4ab00ddae4149451563a31fe2862cfb7ab18320329d186b99e`; artifact ZIP digest `sha256:0851787dacd882e86f398adebd4680493b6693d08865521fb677d7d9ecf3892f`.
- **State owner / shared dependencies**: `ConversationRepository` owns production conversation summaries, selected conversation identity, loaded detail and current visible branch messages. It holds only an in-memory `AuthTransientSession`; `AuthSessionStore` remains auth/account-context owner and default WebKit data store remains persistent auth-secret authority. `ProtocolReadProbe` remains diagnostic-only.
- **Frozen / do-not-touch**: No Frozen modules. Stable auth/account and protocol-read diagnostic contracts remain unchanged. No automatic retry, new headers, alternate endpoints, shadow WebView or persistent copied credentials were added.
- **Parallel/base conflict check**: `docs/project/current/dev/` contains only this Active feature checkpoint plus the README template. PR #9 and branch match this checkpoint. `main` has not advanced from the recorded base. b9/build 9 is unique. Post-artifact branch changes are documentation-only.
- **Completed implementation**: Production repository/state owner; list GET; selected identity ownership; detail GET identity check; current-branch extraction `current_node -> parent`; visible user/assistant text extraction; table-view virtualization; UIKit split sidebar/detail shell; settings/login/list reload entry points; privacy-safe selected-conversation correlation; terminal explicit detail `重新加载`; unique b9 candidate and artifact.
- **b8 runtime evidence**: Exact b8/build 8/source `e312acc3dd17`, Release, iPhone/iOS 17.0. Native shell launched. After explicit login/account verification, production list repeatedly returned HTTP 200 / 28 of total 29. One selected detail GET took `30935.12 ms` and returned HTTP 500 at response stage before parse/render. Initial launch had 0/0 WebKit cookies. b8 is preserved as partial/failing evidence only.
- **b9 runtime evidence**: User exported exact `DEV-native-read-path-0.1.0-b9`, build 9, source `d9c9b4da8bde`, Release, iPhone / iOS 17.0. Initial repository auth probe again observed 0/0 WebKit cookies and `missing_required_session_fields`; after explicit login, WebKit had 48/29 then 49/30 total/matched cookies and Plus/personal account context verified with HTTP 200. Production list returned HTTP 200, 28 items / total 29. Two distinct selected conversation hashes then completed end-to-end detail reads: list position 1 / `sha256:e95144bf259d` returned HTTP 200, 1,529,866 bytes, mapping 337, visible messages 154, `status=ok` in 5,668.41 ms; list position 13 / `sha256:084c1f4870f3` returned HTTP 200, 7,503,328 bytes, mapping 2023, visible messages 843, `status=ok` in 20,742.89 ms. User explicitly confirmed both conversations rendered/read successfully.
- **Evidence interpretation**: Production native list/detail/current-branch message rendering is now runtime/manual/real-device proven for two distinct Plus/personal conversations on iPhone / iOS 17.0, including one 7.50 MB / 2023-node conversation. The earlier b8 HTTP 500 is not evidence of a systematic current implementation failure. It remains an observed transient/conversation-specific-or-server-side failure whose exact cause is unproven.
- **Validation state**: b9 = **Code written + CI passed + Artifact produced + Runtime/manual/real-device tested** for the native shell/list/two-detail/current-branch message read scope. This task's core acceptance criteria are met. `重新加载` is implemented and CI/artifact-proven but was not exercised because both b9 detail reads succeeded; do not separately claim its real-device failure-path behavior is proven. Persistent auth across install/update also remains Unverified because b9 again began with 0/0 cookies.
- **Next exact action**: Update durable project state and PR #9 with b9 runtime acceptance, verify final base/head/product-source identity and docs-only delta after `d9c9b4da...`, mark PR ready and merge if no conflict appears. After merge, remove only this checkpoint. The next serialized core task should be `DEV-conversation-recovery` for broader `同步最新消息` / loaded-state `重载当前会话`, while install/update auth persistence remains a separately evidenced issue.
- **Rejected / do-not-repeat**: No automatic session/detail retry; no UA spoof/Cloudflare bypass; no speculative account/browser headers; no fallback conversation endpoints; no UI-title identity; no naive all-mapping view materialization; no reuse of `ProtocolReadProbe` as production repository; no hidden/shadow WebView.
- **Open questions / risks**: b9 startup again lacked usable WebKit cookies until explicit login, so install/update auth persistence remains Unknown / Unverified. The 20.74 s 7.50 MB detail is a concrete performance signal but current diagnostics do not decompose transport vs JSON parse vs UI update time. Lower iOS, iPad, non-personal workspaces, send/streaming and attachments remain Unverified.
