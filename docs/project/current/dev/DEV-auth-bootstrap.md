# DEV-auth-bootstrap

## Status

**Active**

- **Work ID**: `DEV-auth-bootstrap`
- **Routing aliases / keywords**: `网页登录 / Google 登录 / Google 登录验证 / 登录验证 / auth bootstrap`
- **Task**: Reproduce and verify the current ChatGPT web login path, specifically Continue with Google, on-device before native private-protocol work.
- **User intent / acceptance criteria**: Provide a testable iOS path that can open the current ChatGPT web login, attempt Continue with Google, preserve privacy-safe auth/navigation diagnostics, and establish real-device evidence of whether login succeeds or what current blocking/failure behavior occurs. Use the user-supplied 1024×1024 image as the app icon. Do not claim authenticated native protocol access until separately evidenced.
- **Baseline**: `main` remains at `836651a41e36feafcc2386939d70d673be6e3725`; accepted runtime foundation `DEV-app-foundation-0.1.0-b1`, version `0.1.0 (1)`, TrollStore tested on iPhone / iOS 17.0.
- **Working branch / PR / head commit**: `dev/auth-bootstrap-20260826`; draft PR #6 `DEV-auth-bootstrap: web and Google login verification`; runtime-tested product/build-input head `809fa03e673afded87cb47fb755c998ab1b58e12` before this documentation update.
- **Candidate identity**: `DEV-auth-bootstrap-0.1.0-b2`; version `0.1.0 (2)`; IPA `ChatGPTClient-0.1.0-b2-dev-auth-bootstrap.ipa`; GitHub artifact `ChatGPTClient-DEV-auth-bootstrap-0.1.0-b2`, artifact ID `9577612707`.
- **Evidence**: GitHub Actions run `32886019320` passed on Xcode 16.4 and produced the exact b2 artifact. IPA SHA-256 is `426c5f9b6b5e71a41c3ca571abdc73951835a55dc902691d75030a781ee61465`. The user then installed/tested this candidate and explicitly reported successful ChatGPT login through the embedded web flow using Continue with Google. This current real-device result overrides the earlier risk hypothesis that Google might reject the embedded user-agent for this tested path/device. No system-browser fallback is justified by current evidence.
- **Files / modules in scope**: `ChatGPTClient/Authentication/AuthWebViewController.swift`; `ChatGPTClient/RootViewController.swift`; existing diagnostics authority; `ChatGPTClient.xcodeproj/project.pbxproj`; `ChatGPTClient/Assets.xcassets/**`; `scripts/build_ipa.sh`; `scripts/reconstruct_app_icon.sh`; `scripts/assets/**`; `.github/workflows/ios-foundation.yml`; checkpoint and durable project docs.
- **State owner / shared dependencies**: `AuthWebViewController` currently owns the embedded web-login verification UI/navigation lifecycle and uses the default persistent `WKWebsiteDataStore`. A production authenticated-session/account-context owner is still not established. Depends on Stable application shell and Stable diagnostics foundation.
- **Frozen / do-not-touch**: No modules are Frozen. Do not add a system-browser fallback while the tested embedded route works. Do not assume WebKit auth state is consumable by native `URLSession`; establish that separately from evidence.
- **Parallel conflicts checked against**: No other Active development checkpoint exists; dedicated branch/candidate remain unique; `main` has not advanced. Core roadmap serial dependency remains foundation -> auth -> protocol read.
- **Completed**: Minimal `WKWebView` login bootstrap at `https://chatgpt.com/auth/login`; root login-verification entry; privacy-safe navigation diagnostics; b2 build/candidate allocation; user-supplied app icon integrated and compiled; CI build/artifact production passed; exact b2 IPA produced; user real-device test completed Continue with Google successfully and reached logged-in ChatGPT state.
- **Validation state**: **Code written + CI passed + Artifact produced + Runtime/manual/real-device tested for the embedded ChatGPT/Google web-login path**. Local Xcode/static checks were not available separately. The login route is runtime-validated for the tested iPhone / iOS 17.0 candidate, but the broader authentication/session module is **not Stable/Frozen** because authenticated-state detection, relaunch persistence evidence and native session/account consumption remain unverified.
- **Pending**: Capture the existing privacy-safe diagnostic export from the successful login to pin the navigation result to the exact b2 runtime identity; verify login persistence/re-entry after force-closing and reopening the app; then determine an evidence-backed authenticated/unauthenticated state signal and how session/account context can be consumed by later native requests without logging secret values.
- **Next exact action**: Have the user force-close/reopen `DEV-auth-bootstrap-0.1.0-b2`, enter `开始网页登录验证` again and report whether ChatGPT remains signed in, then export the Settings diagnostic JSON. Inspect only safe navigation/build/runtime metadata from that export before changing authentication/session code.
- **Rejected / do-not-repeat**: The hypothesis that this tested embedded Google route necessarily requires a system-browser fallback is rejected by current real-device success. Do not add speculative fallback/auth retry chains. Do not assume WebKit/system-browser/`URLSession` sessions are interchangeable. Do not log passwords, OAuth codes, tokens, Cookie values or Authorization values. Do not call successful web login proof of authenticated native protocol access.
- **Open questions / risks**: Whether the `WKWebsiteDataStore` session persists across app relaunch is not yet explicitly tested in this candidate; a successful logged-in WebView does not prove native `URLSession` can consume the same session; authenticated-vs-unauthenticated and account/workspace context need a verified state owner before protocol-read implementation can depend on them.
