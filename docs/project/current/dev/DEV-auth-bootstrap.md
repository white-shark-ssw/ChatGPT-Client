# DEV-auth-bootstrap

## Status

**Active — acceptance achieved; pending integration/merge**

- **Work ID**: `DEV-auth-bootstrap`
- **Routing aliases / keywords**: `网页登录 / Google 登录 / Google 登录验证 / 登录验证 / auth bootstrap`
- **Task**: Establish the current ChatGPT login/session bootstrap on real device, including WebKit persistence, transient native session/account consumption, and one explicit account/workspace context owner before native protocol-read work.
- **Acceptance criteria**: Testable embedded Google login, persistent authenticated WebKit state, privacy-safe diagnostics, native session/account context proven on-device, user-supplied app icon, and a Settings control to clear accumulated local diagnostics. Do not claim private conversation protocol support from auth evidence alone.
- **Branch / PR / base**: `dev/auth-bootstrap-20260826`; draft PR #6 open/mergeable; base `main` remains `836651a41e36feafcc2386939d70d673be6e3725` at the last guard. No other Active development checkpoint exists.
- **Accepted foundation**: `DEV-app-foundation-0.1.0-b1`.
- **Accepted auth candidate**: **`DEV-auth-bootstrap-0.1.0-b6`**, version `0.1.0 (6)`, exact product/workflow source `19c0cd22923d8c6f4c96e676258b31814d02a942`, artifact ID `9594474567`, IPA `ChatGPTClient-0.1.0-b6-dev-auth-bootstrap.ipa`, IPA SHA-256 `c7109f691c1de675ef55da1a08695c10663b62030853453ee2fafd01fb070c8b`, artifact ZIP digest `sha256:68c7cfc6667c362c79900be1cf46154a76aa3a363649b1995ff02a5d83b88d85`.
- **CI/artifact evidence**: Authoritative push run `32934821144` passed Xcode 16.4 build/inspect/upload. Build logs embedded `DIAGNOSTICS_CANDIDATE=DEV-auth-bootstrap-0.1.0-b6`, `SOURCE_COMMIT=19c0cd22923d`, compile target `arm64-apple-ios14.0`, and `BUILD SUCCEEDED`. Downloaded artifact identity/checksum were independently rechecked.
- **b2 accepted evidence**: Embedded Continue with Google succeeded on iPhone / iOS 17.0 and default persistent `WKWebsiteDataStore` retained login across force-close/relaunch. WebKit remains the only persistent auth-secret authority.
- **b3/b4 durable conclusion**: Native browser-oriented `/auth/login` is route-specific diagnostic evidence only. b3 once returned authenticated HTTP 200; b4 later returned Cloudflare HTTP 403 while WebKit itself remained authenticated. It is not an account-context gate or state authority.
- **b5 durable conclusion**: Direct account probing is the correct path. One b5 probe returned `/api/auth/session` HTTP 403 after a Cloudflare challenge; a later b5 run returned `/api/auth/session` HTTP 200 and bearer-authenticated accounts-check HTTP 200, then failed only because the parser expected obsolete `accounts.default.account.id`.
- **b6 code**: `AuthSessionStore` parses ordered-account response shape using `account_ordering` + keyed `accounts` + nested `account.account_id`, selecting the first ordered entry not explicitly denied by `can_access_with_session`; optional `plan_type` / `structure` remain metadata. No speculative retry/fallback, User-Agent spoof, Cloudflare bypass, browser-script token extraction or second credential authority was added.
- **b6 exact runtime evidence**: User export `ChatGPTClient-Diagnostics-20260826-055035.json` identifies candidate b6, app `0.1.0 (6)`, source `19c0cd22923d`, Release, iPhone / iOS 17.0. First attempt at 05:50:19Z used 48 total / 29 matched WebKit cookies and `/api/auth/session` returned HTTP 403 (`stage=session`, `session.accountState=notAvailable`). The user then explicitly restarted verification; the second attempt at 05:50:25Z used 49 total / 30 matched cookies, `/api/auth/session` returned HTTP 200, accounts-check returned HTTP 200, parser observed `accountCount=2`, `accountOrderingCount=1`, selected an account with `planType=plus`, `structure=personal`, set `session.accountState=verified`, and ended `accountContextProbe` with `status=ok` in 1289.71 ms. User screenshot title also reads `登录会话 · 账户上下文通过`.
- **Runtime interpretation**: **Account/workspace context is now Runtime/manual/real-device tested and accepted for the current b6 path on iPhone / iOS 17.0.** The first 403 remains important route/timing evidence; the successful second attempt was user-triggered via `重新开始`, not an automatic retry added by the app.
- **State owners**: Default persistent `WKWebsiteDataStore` = sole persistent auth-secret authority. `AuthSessionStore` = accepted in-memory auth/account context owner for the current path; `/api/auth/session` bearer remains transient/local only. `DiagnosticsLogger` / `DiagnosticsStore` / exporter remain the sole diagnostics authority.
- **Diagnostics clear**: b6 Settings includes `清理诊断日志`, clearing `events.jsonl` plus configured rotated archives through the existing store queue and not touching WebKit/auth state. The supplied b6 export contains only the fresh 05:50 test cycle and no older b1-b5 events, which is consistent with the requested clean-log workflow; the clear operation intentionally does not append a replacement event.
- **Files/modules changed**: Authentication bootstrap/session store, diagnostics clear path, Settings UI, app icon assets/build reconstruction, build identity/CI, and project docs. No private conversation protocol implementation is included.
- **Validation state**: b6 = **Code written + CI passed + Artifact produced + Runtime/manual/real-device tested** for embedded authenticated re-entry, direct session request success, accounts-check success, and ordered account-context parsing. Authentication/account context is accepted/Stable for this exact scope; lower iOS versions, iPad, workspace behavior beyond the selected account, and private conversation protocol remain Unverified.
- **Rejected artifact**: First b4 artifact from run `32891478482` / artifact ID `9579620441` remains invalid because the embedded diagnostic candidate identity was b3.
- **Rejected / do-not-repeat**: Treating any single `/auth/login` result as durable auth authority; describing the first b6 403 as the final result; restoring `accounts.default.account.id`; speculative retries/timers/fallback endpoints; User-Agent spoofing; Cloudflare bypass; browser-script token extraction; persistent copied Cookie/session/bearer storage; logging passwords/OAuth codes/tokens/Cookie/Authorization/response bodies.
- **Next exact action**: Finalize PR #6 for integration: re-check `main`/PR head and conflicts, then merge only with explicit user approval. After auth is integrated, a separate `DEV-protocol-read` task may begin current conversation-list/detail protocol evidence work using the accepted b6 account context. Do not start protocol implementation in this checkpoint.
