# DEV-send-stream

## Status

**Active**

- **Work ID**: `DEV-send-stream`
- **Routing aliases / keywords**: `Send/Stream / 发送 / 流式回复 / 新对话 / Stop / reasoning / follow-tail`
- **Task**: Implement the first evidence-backed production text Send/new-conversation/streaming-response path, including exact response ownership, Stop integration, user-visible reasoning compatibility, follow-tail and new-chat identity handoff.
- **User intent / acceptance criteria**: Start Phase 9 now and continue autonomously through evidence-backed implementation, CI/package identity and a uniquely identified TrollStore IPA until a genuine real-device/human-only gate is reached. Do not guess current private ChatGPT protocol behavior; preserve one conversation authority and existing Stable b38 presentation behavior.
- **Baseline**: `main@34811877896ca88c6656be6676f5466a19931ce6`; predecessor `DEV-conversation-round-count` completed/merged Stable b38, PR #27 merge `9110c9e893e8a8665c7a58cf27bb42c65a39cc11`; exact accepted Runtime Candidate `DEV-conversation-round-count-0.1.0-b38`, tested product source `0d1801137e4ee2f5889ca718cd8b2e3612bdaa67`, Runtime Artifact `9708425762`.
- **Working branch / PR / head commit**: `dev/send-stream-20260829`; branch created from `34811877896ca88c6656be6676f5466a19931ce6`; checkpoint activation commit `e82975dc04c210eaa5029eb611f6acd2f10eca57`; PR not yet opened.
- **Candidate identity**: Not allocated. Current Xcode source is `0.1.0 (38)` / b38. b24-b38 are permanently reserved. b39 appears unclaimed by Active checkpoints/open PRs, but will be written into product config only once the first testable Send/Stream evidence candidate is justified.
- **Evidence**: Current `main` has no other Active development checkpoint and no open PR. PR #27 is merged. `PROJECT_STATE.md`/`DEVELOPMENT_PLAN.md` identify Phase 9 as `DEV-send-stream`. Current `main` unexpectedly lacks `docs/project/SEND_STREAM_PREFLIGHT.md` even though current project docs require it; the file exists on pre-merge `main@a6e3b2bc...`. Source audit confirms `ConversationDetail.currentNodeID` survives the read projection, `ConversationRepository` is still the sole account-scoped resident/read/recovery owner, `protectedResidentKeys()` currently protects detail operations + selected resident only, `ConversationDetailViewController` has no composer/response/follow-tail owner yet, `RootViewController` collapses to primary whenever `selectedConversationID == nil`, and `AuthTransientSession` exposes only completion-handler `URLSessionDataTask` delivery.
- **Current protocol research**: Recent 2026 public implementations/captures consistently identify current ChatGPT Web generation as `POST /backend-api/f/conversation`, generally preceded by Sentinel chat-requirements and `/f/conversation/prepare`; direct replay without valid browser-generated requirements/proof is reported to HTTP403. Current SSE evidence uses `data:` frames with protocol marker `"v1"`, patch/event objects, conversation/message identity fields and `[DONE]`; common text patches append `/message/content/parts/0`, and terminal message state may become `finished_successfully`. This is useful external structural evidence, **not accepted account/runtime proof**. Exact request/body/header requirements, Stop route and reasoning semantics for this product account remain Unknown/Unverified.
- **Evidence-backed decision**: Do **not** implement Sentinel/PoW/Turnstile bypass or replay captured proof credentials. Before production Send, build a visible diagnostic-only Web protocol probe that loads the official `chatgpt.com` page in the existing default WebKit data store and observes only privacy-safe structural metadata while the official page performs its own normal requirements flow. The probe must not become product chat transport, a hidden/shadow WebView, a second auth store, or persist any request/response body/token/header value.
- **Files / modules in scope**: primary future production `ChatGPTClient/Conversation/ConversationFeature.swift`, `ChatGPTClient/RootViewController.swift`; `ChatGPTClient/Authentication/AuthSessionStore.swift` only if accepted stream evidence later requires incremental authenticated transport; immediate evidence batch may add one smallest `ChatGPTClient/Protocol/ProtocolSendProbeViewController.swift` and a Settings diagnostic entry; Xcode project only to register that file; build/workflow/package identity only at Candidate stage; selected task checkpoint and affected durable docs.
- **State owner / shared dependencies**: `ConversationRepository` remains sole conversation/list/read/recovery/response authority; `AuthSessionStore` remains auth/account authority; default persistent `WKWebsiteDataStore` remains sole persistent auth-secret authority; `ConversationDetailViewController` remains viewport/scroll-presentation owner; b38 deterministic message presentation geometry/round-navigation contracts must remain intact.
- **Frozen / do-not-touch**: No module is Frozen, but do not replace Stable b38 geometry/round-navigation architecture without new evidence. Do not create a second repository/global `isStreaming`, persist copied auth secrets, expose hidden chain-of-thought, add retry/timer/watchdog/fallback endpoints, bypass/solve service anti-bot protections in product code, or make Sync/Reload resend prompts.
- **Parallel conflicts checked against**: `docs/project/current/dev/` contains only README on `main`; no open PRs; predecessor PR #27 merged and checkpoint removed. Old `rules/send-stream-preflight-20260828` is planning-only and not a development branch/candidate.
- **Completed**: Re-read current governance/startup and required project docs; activated the explicit new Work; verified predecessor merge/current main/no parallel conflict/build identity; created dedicated branch + checkpoint; audited merged b38 `ConversationFeature.swift`, `RootViewController.swift`, `AuthSessionStore.swift`, `ProtocolReadProbe.swift`, Settings and Xcode source registration; researched recent current Web Send route/framing/protection evidence; rejected guessed direct production Send and selected a visible diagnostic-only evidence path.
- **Validation state**: Development Work activated + source/protocol audit completed. No production Send code, Candidate, CI, Artifact or Runtime claim yet.
- **Pending**: Restore/revalidate `SEND_STREAM_PREFLIGHT.md` and START_HERE routing; implement the privacy-safe visible Web protocol probe; then allocate first unique evidence Candidate, CI/package it and ask for the minimum real-device probe action. Production request/stream/Stop implementation begins only after that current account evidence is available.
- **Next exact action**: Batch B1: restore a current b38-aware `docs/project/SEND_STREAM_PREFLIGHT.md` and explicit START_HERE route; verify actual branch content. Then Batch B2: add the visible diagnostic probe and Settings entry without touching production conversation ownership.
- **Rejected / do-not-repeat**: Do not reuse historical `/backend-api/conversation` Send assumptions; do not infer that a current public reverse-engineered request is sufficient proof for this account; do not implement Sentinel/PoW/Turnstile bypass/replay; do not treat local `URLSessionTask.cancel()` as server Stop; do not globally serialize unrelated conversations; do not allocate/reuse an emitted Candidate under corrected code; do not replay failed placeholder PR creation attempts (both were HTTP422/no mutation).
- **Open questions / risks**: Exact current-account Send/new-chat request shape, server Stop route/ack, user-visible reasoning event semantics and whether final Detail reconciliation is needed remain Unknown/Unverified. The diagnostic candidate requires a real device/user message submission in the visible official page; that will be a genuine human-only evidence gate after CI/IPA production.

## Batch recovery point

Confirmed complete: branch creation + activation checkpoint + **Batch A source/protocol audit**. Do not replay those writes. Planned remaining batches: **B1** restore/revalidate durable Send/Stream preflight + START_HERE route; **B2** add visible privacy-safe protocol probe + Settings/Xcode registration; **D1** only after B2 is source-reviewed, allocate the first unique Candidate (b39 only if still unclaimed), update product/workflow identity atomically enough for one intended source, run CI/package and independently inspect IPA identity; **Runtime gate** user opens the visible protocol probe and sends one harmless existing/new-chat test message through the official page, then exports diagnostics; **C** use that exact evidence to implement production per-conversation response ownership/streaming/new-chat/Stop/follow-tail; further Candidates must use new unique builds. After interruption, re-read this checkpoint + real branch/head and perform only missing deterministic writes. Never blindly replay Candidate/package writes. Recovery must not touch another Work checkpoint/branch/identity.