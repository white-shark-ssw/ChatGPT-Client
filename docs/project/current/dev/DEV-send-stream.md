# DEV-send-stream

## Status

**Active**

- **Work ID**: `DEV-send-stream`
- **Routing aliases / keywords**: `Send/Stream / 发送 / 流式回复 / 新对话 / Stop / reasoning / follow-tail`
- **Task**: Implement the first evidence-backed production text Send/new-conversation/streaming-response path, including exact response ownership, Stop integration, user-visible reasoning compatibility, follow-tail and new-chat identity handoff.
- **User intent / acceptance criteria**: Start Phase 9 now and continue autonomously through evidence-backed implementation, CI/package identity and a uniquely identified TrollStore IPA until a genuine real-device/human-only gate is reached. Do not guess current private ChatGPT protocol behavior; preserve one conversation authority and existing Stable b38 presentation behavior.
- **Baseline**: `main@34811877896ca88c6656be6676f5466a19931ce6`; predecessor `DEV-conversation-round-count` completed/merged Stable b38, PR #27 merge `9110c9e893e8a8665c7a58cf27bb42c65a39cc11`; exact accepted Runtime Candidate `DEV-conversation-round-count-0.1.0-b38`, tested product source `0d1801137e4ee2f5889ca718cd8b2e3612bdaa67`, Runtime Artifact `9708425762`.
- **Working branch / PR / head commit**: `dev/send-stream-20260829`; branch created from `34811877896ca88c6656be6676f5466a19931ce6`; PR not yet opened; checkpoint commit pending verification.
- **Candidate identity**: Not allocated. Current Xcode source is `0.1.0 (38)` / b38. b24-b38 are permanently reserved. b39 appears unclaimed by Active checkpoints/open PRs, but will be written into product config only once the first testable Send/Stream candidate is justified.
- **Evidence**: Current `main` has no other Active development checkpoint and no open PR. PR #27 is merged. `PROJECT_STATE.md`/`DEVELOPMENT_PLAN.md` identify Phase 9 as `DEV-send-stream`. Current `main` unexpectedly lacks `docs/project/SEND_STREAM_PREFLIGHT.md` even though current project docs require it; the file exists on pre-merge `main@a6e3b2bc...` and must be restored/revalidated against b38 before relying on it. Current accepted private protocol evidence covers read only; Send/new-chat/stream/Stop/reasoning remain Unknown/Unverified.
- **Files / modules in scope**: primary `ChatGPTClient/Conversation/ConversationFeature.swift`, `ChatGPTClient/RootViewController.swift`; `ChatGPTClient/Authentication/AuthSessionStore.swift` only if current stream evidence requires incremental authenticated transport; smallest protocol diagnostic/transport file only if justified; build/workflow/package identity files only at Candidate stage; selected task checkpoint and affected durable docs.
- **State owner / shared dependencies**: `ConversationRepository` remains sole conversation/list/read/recovery/response authority; `AuthSessionStore` remains auth/account authority; default persistent `WKWebsiteDataStore` remains sole persistent auth-secret authority; `ConversationDetailViewController` remains viewport/scroll-presentation owner; b38 deterministic message presentation geometry/round-navigation contracts must remain intact.
- **Frozen / do-not-touch**: No module is Frozen, but do not replace Stable b38 geometry/round-navigation architecture without new evidence. Do not create a second repository/global `isStreaming`, persist copied auth secrets, expose hidden chain-of-thought, add retry/timer/watchdog/fallback endpoints, or make Sync/Reload resend prompts.
- **Parallel conflicts checked against**: `docs/project/current/dev/` contains only README on `main`; no open PRs; predecessor PR #27 merged and checkpoint removed. Old `rules/send-stream-preflight-20260828` is planning-only and not a development branch/candidate.
- **Completed**: Re-read current `AGENTS.md` and `START_HERE.md`; routed explicit new Development task; read required current project/profile/state/module/decision/index/rules/documentation/architecture/multi-conversation/development-plan evidence; verified predecessor merge and current main; verified no Active development checkpoint/open PR; inspected Xcode current build identity; created dedicated branch.
- **Validation state**: Development Work activated; no product code written yet; no Candidate/CI/Artifact/Runtime claim.
- **Pending**: Re-read merged b38 source owners; restore and revalidate `SEND_STREAM_PREFLIGHT.md`; establish current Send protocol evidence for existing + new conversation and stream/Stop semantics before production request logic; then make the smallest owner-correct implementation.
- **Next exact action**: Inspect current `ConversationFeature.swift`, `RootViewController.swift`, `AuthSessionStore.swift`, `ProtocolReadProbe.swift` and workflow/build identity on this branch, focusing on current node identity, transport API, new-chat shell assumptions, presentation projections and safe diagnostic insertion points.
- **Rejected / do-not-repeat**: Do not reuse historical endpoint/body guesses; do not infer SSE/WebSocket framing; do not treat local `URLSessionTask.cancel()` as server Stop; do not globally serialize unrelated conversations; do not allocate/reuse an emitted Candidate under corrected code; do not replay failed placeholder PR creation attempts (both were HTTP422/no mutation).
- **Open questions / risks**: Exact private Send/new-chat/stream/Stop request and event grammar is still Unknown/Unverified. The normal implementation may reach a real human-only gate if authenticated current traffic cannot be evidenced without a device action. `SEND_STREAM_PREFLIGHT.md` was lost from current main during predecessor merge and needs durable restoration.

## Batch recovery point

This task uses non-atomic GitHub writes. Confirmed so far: development branch `dev/send-stream-20260829` exists from base `34811877896ca88c6656be6676f5466a19931ce6`; this checkpoint creation is the next write. Planned batches: **A** checkpoint + source/preflight audit; **B** restore/revalidate durable Send/Stream preflight and add only evidence-gathering code if needed; **C** evidence-backed production implementation; **D** allocate unique Candidate/config + CI/package + identity verification; **E** Runtime handoff/docs/merge when accepted. After interruption, re-read this checkpoint and real branch/head, then perform only missing deterministic writes. Never replay already-emitted Candidate/package writes blindly. Recovery must not touch another Work checkpoint/branch/identity.