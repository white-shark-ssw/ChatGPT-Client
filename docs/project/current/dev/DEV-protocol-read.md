# DEV-protocol-read

## Status

**Active**

- **Work ID**: `DEV-protocol-read`
- **Routing aliases / keywords**: `协议读取 / 会话列表 / 会话详情 / conversation list / conversation detail / protocol read`
- **Task**: Establish current ChatGPT conversation-list and conversation-detail protocol evidence using the merged, runtime-tested authentication/account-context baseline before production conversation models or native read UI are implemented.
- **User intent / acceptance criteria**: Start the next core development phase after successful auth. Build the smallest authenticated diagnostic harness that can load the current conversation list and one conversation detail on the intended iPhone, while recording privacy-safe structural/status/timing evidence only. Do not log message bodies or auth secrets and do not infer streaming/send behavior from read-path evidence.
- **Baseline / resume identity**: `main` exact start commit `5547b32ed8c794052eb80bb751abf33121b62926`. Auth PR #6 is merged; merge commit `78f42a06e6254088e3b495cb4529e549a1d4717f`. Accepted auth runtime baseline is `DEV-auth-bootstrap-0.1.0-b6`, product source `19c0cd22923d8c6f4c96e676258b31814d02a942`, iPhone / iOS 17.0.
- **Working branch / PR / head**: `dev/protocol-read-20260826`; PR not created yet; branch starts exactly from `main` `5547b32ed8c794052eb80bb751abf33121b62926`.
- **Candidate identity**: Not allocated. Before the first runnable artifact, re-check `BUILD_TEST_INDEX.md`, all Active checkpoints, Xcode build number/candidate settings, CI artifact names and current branch head. Do not reuse b1-b6 identities.
- **Accepted auth dependency**: Default persistent `WKWebsiteDataStore` is the sole persistent auth-secret authority. `AuthSessionStore` owns accepted in-memory account context; copied cookies and `/api/auth/session` bearer are transient only. Current direct auth path can be challenge-sensitive; no speculative automatic retry is part of the architecture.
- **Protocol evidence boundary**: Conversation-list/detail path, method, query/pagination, required headers/account context, response shapes, node/message/branch semantics and failure behavior are all **Unknown / Unverified** until current evidence is gathered. Historical protocol material is reference-only.
- **Diagnostics/privacy boundary**: Reuse `DiagnosticsLogger` / `DiagnosticsStore` / exporter. Allowed evidence includes request category/path label, HTTP status, duration, byte/item counts, pagination metadata, safe structural counts and hashed conversation/message/account identifiers where needed. Do not log/export full conversation titles when avoidable, message bodies, prompt/response bodies, Cookie values, bearer/token values or Authorization headers.
- **Files/modules initial scope**: merged `ChatGPTClient/Authentication/AuthSessionStore.swift`; `AuthWebViewController.swift` only where the current authenticated WebKit/account handoff requires an explicit test entry; existing diagnostics; a new minimal protocol-read diagnostic owner/UI if evidence justifies it; Xcode/build/CI identity only when a runnable candidate is produced; project docs.
- **State ownership**: Authentication/account ownership remains unchanged. This task may introduce a narrow **protocol diagnostic probe owner** for temporary list/detail evidence, but must not create a production conversation repository/store or second auth authority. Production conversation identity/state ownership belongs to later `DEV-native-read-path` after protocol evidence is accepted.
- **Stable/Frozen impact**: Auth/account context and diagnostics are Stable, not Frozen. They may be consumed by this task; changes to those owners require concrete protocol evidence and must remain minimal. No Frozen modules recorded.
- **Parallel/conflict preflight**: After auth checkpoint completion/removal, `docs/project/current/dev/` on `main` contains only the template; no other Active development checkpoint exists. No branch/candidate conflict exists. Core roadmap is intentionally serialized `foundation -> auth -> protocol read -> native read -> send/stream`.
- **Completed this cycle**: Re-read repository governance entry files; verified PR #6/auth integration; merged PR #6 with user approval; removed completed auth checkpoint; refreshed merged-auth durable state; verified `main` at `5547b32ed8c794052eb80bb751abf33121b62926`; verified no other Active DEV checkpoints; created isolated `dev/protocol-read-20260826` branch.
- **Validation state**: Governance/baseline only. No protocol-read code written, no current list/detail endpoint accepted, no CI/artifact for this task, and no real-device protocol test yet.
- **Next exact action**: Inspect the merged auth/session interfaces, diagnostics API, current shell/build integration and current external evidence for conversation-list/detail requests. Then choose the smallest evidence-backed diagnostic entry point. Do not allocate a build candidate until the first runnable artifact is actually ready.
- **Rejected / do-not-repeat**: Guessing endpoints/shapes from old history; copying browser/session secrets into persistent native storage; logging payload bodies; speculative retry/fallback/timer/UA spoof/Cloudflare bypass; creating production conversation models before list/detail shape evidence; treating auth success as conversation-protocol proof.
- **Open risks / questions**: Exact current list/detail URLs, pagination/query parameters, account header requirements and response structures are unverified. Direct native requests may encounter route-specific challenge behavior. Current workspace semantics beyond the selected personal account remain unverified. Lower iOS versions and iPad remain unverified.
