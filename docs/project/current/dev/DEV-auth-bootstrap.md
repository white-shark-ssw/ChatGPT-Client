# DEV-auth-bootstrap

## Status

**Active**

- **Work ID**: `DEV-auth-bootstrap`
- **Routing aliases / keywords**: `网页登录 / Google 登录 / Google 登录验证 / 登录验证 / auth bootstrap`
- **Task**: Establish the current ChatGPT login/session bootstrap on real device, including WebKit persistence, native-session consumption evidence, and the account/workspace context required before native private-protocol read work.
- **User intent / acceptance criteria**: Provide a testable iOS login path, preserve privacy-safe diagnostics, prove session persistence and native consumption from current evidence, then establish one explicit account/workspace context owner before protocol-read work. Use the user-supplied app icon. Do not claim private conversation-protocol authentication until separately evidenced. Settings must provide a control to clear accumulated local diagnostics during repeated device testing.
- **Baseline / resume identity**: `main` base remains `836651a41e36feafcc2386939d70d673be6e3725`; accepted Stable foundation is `DEV-app-foundation-0.1.0-b1`. Working branch `dev/auth-bootstrap-20260826`; draft PR #6 remains open/mergeable. b5 exact product/workflow source is `c09f981171b02dc8a4f0d8ada4624bd779c68c2f`. **Current b6 exact product/workflow source is `19c0cd22923d8c6f4c96e676258b31814d02a942`**; later commits after it are documentation-only unless separately stated.
- **Candidate identities**: b2 retained for web-login/persistence evidence. b3 retained as historical native `/auth/login` success evidence under its tested conditions. b4 is runtime-tested and failed specifically at the native `/auth/login` Cloudflare gate before account-context probing. b5 `DEV-auth-bootstrap-0.1.0-b5`, version `0.1.0 (5)`, is runtime-tested and establishes direct session/accounts transport plus a response-shape mismatch. Current valid test candidate is **`DEV-auth-bootstrap-0.1.0-b6`**, version `0.1.0 (6)`, IPA `ChatGPTClient-0.1.0-b6-dev-auth-bootstrap.ipa`, artifact `ChatGPTClient-DEV-auth-bootstrap-0.1.0-b6`.
- **Accepted b3 evidence**: User screenshot showed `网页登录成功 · 原生会话通过`; diagnostics matched b3/build 3/source `0fcf040012c0`, iPhone / iOS 17.0, with 54 total / 35 matched WebKit cookies and native `/auth/login` final `chatgpt.com` HTTP 200. This remains valid route/time evidence only.
- **b4 exact runtime evidence**: User-supplied export identifies `DEV-auth-bootstrap-0.1.0-b4`, app `0.1.0 (4)`, source `33ea1b96f755`, Release, iPhone / iOS 17.0. WebKit `/auth/login` returned HTTP 403, then `challenges.cloudflare.com` HTTP 200, then WebKit reached non-`/auth` `chatgpt.com` HTTP 200 and recorded `session.webState=authenticated`. Native ephemeral `/auth/login` copied 46 total / 27 matching ChatGPT/OpenAI cookies but returned HTTP 403 in 536.82 ms and set `session.nativeState=notAuthenticated`. Account probe never started; b4 therefore isolated a failed prerequisite design, not an account-context endpoint failure.
- **b5 exact runtime identity**: User export `ChatGPTClient-Diagnostics-20260826-052835.json` identifies candidate `DEV-auth-bootstrap-0.1.0-b5`, app `0.1.0 (5)`, source `c09f981171b0`, Release, iPhone / iOS 17.0. This matches the authoritative b5 artifact identity.
- **b5 first direct-probe result**: At 05:27:47Z, after WebKit completed the Cloudflare flow and reached authenticated ChatGPT, `accountContextProbe.webData` recorded 46 total / 27 matching cookies. Direct `/api/auth/session` returned HTTP 403 in 555.66 ms and ended `stage=session`, `session.accountState=notAvailable`. This is route/time evidence that the direct session endpoint can still be challenge-sensitive immediately after a WebKit challenge.
- **b5 second direct-probe result**: At 05:28:25Z, WebKit reached non-`/auth` `chatgpt.com` HTTP 200. The account probe recorded 49 total / 30 matching cookies. `/api/auth/session` returned HTTP 200 and produced the expected user id + transient access token. The subsequent `/backend-api/accounts/check/v4-2023-04-27` request also returned HTTP 200, but b5 ended `stage=accounts`, `reason=missing_default_account` because b5 parsed `payload.accounts.default.account.id`.
- **Critical b5 interpretation**: The second b5 run proves the current ephemeral native transport can successfully perform `/api/auth/session` and the bearer-authenticated accounts-check request on the intended device. The remaining observed failure is response parsing, not HTTP authentication/transport. Current evidence for the same accounts-check endpoint shows `account_ordering` plus an `accounts` dictionary keyed by account identity, with nested `account.account_id`; this is the basis for the b6 parser correction. No retry, alternate endpoint, browser-token extraction, User-Agent spoofing or Cloudflare bypass is justified.
- **b6 code written**: `AuthSessionStore` now requires `account_ordering`, iterates ordered account keys in `accounts`, skips entries explicitly marked `can_access_with_session=false`, and reads the first usable nested `account.account_id`; optional `plan_type` / `structure` remain unchanged. Safe failure metadata records only reason and account/order counts. `DiagnosticsStore` now clears `events.jsonl` plus all configured rotated archives on its existing serial queue; `DiagnosticsLogger.clearStoredLogs()` exposes that operation. `SettingsViewController` adds `清理诊断日志`, showing success/failure UI without clearing WebKit/auth state or creating another log authority.
- **Diagnostics clear semantics**: Clearing removes the current local JSONL and rotated archives owned by `DiagnosticsStore`; it does not alter WebKit cookies/login state, auth/account state, application settings, or temporary exported JSON files. The clear operation itself does not append a replacement persistent diagnostics event, so the store is actually empty immediately after success; subsequent app events begin accumulating normally.
- **State owner / shared dependencies**: Default persistent `WKWebsiteDataStore` remains the sole persistent auth-secret authority. `AuthSessionStore` owns safe auth evidence and Candidate in-memory account context. `/api/auth/session` bearer is transient/local only. `DiagnosticsLogger` / `DiagnosticsStore` remain the sole diagnostics authority; the clear action operates on that owner only.
- **Files / modules changed for b6**: `ChatGPTClient/Authentication/AuthSessionStore.swift`; `ChatGPTClient/Diagnostics/Diagnostics.swift`; `ChatGPTClient/SettingsViewController.swift`; `ChatGPTClient.xcodeproj/project.pbxproj`; `scripts/build_ipa.sh`; `.github/workflows/ios-foundation.yml`; checkpoint/durable docs. Diagnostics is Stable but not Frozen; touching it is justified by the user's explicit repeated-test log-clear requirement.
- **Parallel conflicts checked**: `docs/project/current/dev/` contains no other Active development checkpoint. PR #6 branch/base remain unique; b6 version/build/candidate/artifact identity is unique against b1-b5. `main` remains at `836651a...`. Core roadmap remains serialized foundation -> auth -> protocol read.
- **Rejected artifact**: First b4 artifact from run `32891478482` / artifact ID `9579620441` remains invalid because filename/artifact said b4 while embedded diagnostic candidate was b3. Do not use it.
- **Valid b5 CI/artifact evidence**: Authoritative push run `32932389742` at source `c09f981171b02dc8a4f0d8ada4624bd779c68c2f`; artifact ID `9593649485`; IPA SHA-256 `d9a22635cc6ac05d2ba09a0a627eaa74d38d1a690b5e9affe2f318d2aa204f15`.
- **Valid b6 CI/artifact evidence**: Authoritative push run `32934821144` at exact product/workflow source `19c0cd22923d8c6f4c96e676258b31814d02a942` passed Xcode 16.4 build/inspect/upload. Logs show `SOURCE_COMMIT=19c0cd22923d`, `DIAGNOSTICS_CANDIDATE=DEV-auth-bootstrap-0.1.0-b6`, compile target `arm64-apple-ios14.0`, `BUILD SUCCEEDED`, and output candidate b6. Artifact ID `9594474567`; artifact `ChatGPTClient-DEV-auth-bootstrap-0.1.0-b6`; IPA `ChatGPTClient-0.1.0-b6-dev-auth-bootstrap.ipa`; IPA SHA-256 `c7109f691c1de675ef55da1a08695c10663b62030853453ee2fafd01fb070c8b`; artifact ZIP digest `sha256:68c7cfc6667c362c79900be1cf46154a76aa3a363649b1995ff02a5d83b88d85`.
- **Local b6 artifact identity check**: Downloaded artifact was extracted locally. Embedded Info.plist confirms app `0.1.0 (6)`, `DiagnosticsCandidate=DEV-auth-bootstrap-0.1.0-b6`, `DiagnosticsSourceCommit=19c0cd22923d`, Release, minimum OS 14.0. Local IPA SHA-256 equals the CI value `c7109f691c1de675ef55da1a08695c10663b62030853453ee2fafd01fb070c8b`.
- **Validation state**: b2/b3 retain accepted historical scopes. b4 is Code written + CI passed + Artifact produced + Runtime/manual/real-device tested with native `/auth/login` Cloudflare 403. b5 is **Code written + CI passed + Artifact produced + Runtime/manual/real-device tested**: first direct session request 403 after challenge; second direct session request HTTP 200 and accounts-check HTTP 200, then parser failure `missing_default_account`. b6 is **Code written + CI passed + Artifact produced; Runtime/manual/real-device not yet tested**. Account/workspace context remains Candidate / Unverified until b6 parsing succeeds on device.
- **Next exact action**: Install exact b6 artifact ID `9594474567`. Optionally open Settings and press `清理诊断日志` first to remove accumulated b1-b5 diagnostics. Then open auth verification once and wait for the final title. Success target remains `登录会话 · 账户上下文通过`; on any other result, export one fresh b6 diagnostic JSON and interpret its exact stage/reason before changing code.
- **Rejected / do-not-repeat**: Treating b5 accounts HTTP 200 as authentication failure; preserving the unsupported `accounts.default.account.id` shape after current evidence contradicts it; system-browser fallback; speculative endpoint fallback/retry/timer chains; User-Agent spoofing; Cloudflare bypass; browser-script token extraction without new evidence; persistent copied Cookie/session/bearer storage; logging passwords, OAuth codes, tokens, Cookie values, Authorization values or response bodies.
- **Open questions / risks**: The b6 ordered-account parser is current-evidence-driven but still requires real-device acceptance. A fresh `/api/auth/session` request can return 403 immediately after a Cloudflare challenge, as the first b5 probe showed; no retry is added without new evidence. Workspace semantics beyond the selected account remain Unknown / Unverified. Lower iOS versions, iPad and conversation protocol remain unverified.
