# DEV-auth-bootstrap

## Status

**Active**

- **Work ID**: `DEV-auth-bootstrap`
- **Routing aliases / keywords**: `网页登录 / Google 登录 / Google 登录验证 / 登录验证 / auth bootstrap`
- **Task**: Establish the current ChatGPT login/session bootstrap on real device, including WebKit persistence, native-session consumption evidence, and the account/workspace context required before native private-protocol read work.
- **User intent / acceptance criteria**: Provide a testable iOS login path, preserve privacy-safe diagnostics, prove session persistence and native consumption from current evidence, then establish one explicit account/workspace context owner before protocol-read work. Use the user-supplied app icon. Do not claim private conversation-protocol authentication until separately evidenced.
- **Baseline / resume identity**: `main` base remains `836651a41e36feafcc2386939d70d673be6e3725`; accepted Stable foundation is `DEV-app-foundation-0.1.0-b1`. Working branch `dev/auth-bootstrap-20260826`; draft PR #6 remains open/mergeable. Exact b5 product/workflow source is `c09f981171b02dc8a4f0d8ada4624bd779c68c2f`; commits after it are documentation-only unless separately stated.
- **Candidate identities**: b2 retained for web-login/persistence evidence. b3 retained as historical native `/auth/login` success evidence under its tested conditions. b4 `DEV-auth-bootstrap-0.1.0-b4`, version `0.1.0 (4)`, is runtime-tested and failed specifically at the native `/auth/login` Cloudflare gate before account-context probing. Current valid test candidate is **`DEV-auth-bootstrap-0.1.0-b5`**, version `0.1.0 (5)`, IPA `ChatGPTClient-0.1.0-b5-dev-auth-bootstrap.ipa`, artifact `ChatGPTClient-DEV-auth-bootstrap-0.1.0-b5`.
- **Accepted b3 evidence**: User screenshot showed `网页登录成功 · 原生会话通过`; diagnostics matched b3/build 3/source `0fcf040012c0`, iPhone / iOS 17.0, with 54 total / 35 matched WebKit cookies and native `/auth/login` final `chatgpt.com` HTTP 200. This remains valid route/time evidence only.
- **b4 exact runtime evidence**: User-supplied export identifies `DEV-auth-bootstrap-0.1.0-b4`, app `0.1.0 (4)`, source `33ea1b96f755`, Release, iPhone / iOS 17.0. WebKit `/auth/login` returned HTTP 403, then `challenges.cloudflare.com` HTTP 200, then WebKit reached non-`/auth` `chatgpt.com` HTTP 200 and recorded `session.webState=authenticated`. Native ephemeral `/auth/login` copied 46 total / 27 matching ChatGPT/OpenAI cookies but returned HTTP 403 in 536.82 ms and set `session.nativeState=notAuthenticated`. Screenshot shows `网页登录成功 · 原生会话未通过`.
- **b4 interpretation**: Persistent WebKit login was not lost. `/api/auth/session` and accounts-check were not tested in b4 because `AuthWebViewController` gated `startAccountProbeIfNeeded()` behind native `/auth/login` success; no `accountContextProbe.requested` / `session.accountState` event exists. Therefore b4 is a failed prerequisite design, not an account-context endpoint failure.
- **b5 code written**: `AuthWebViewController` no longer runs native `/auth/login` as a prerequisite. After WebKit actually finishes at authenticated non-`/auth` `chatgpt.com`, it directly starts `probeAccountContext`. `AuthSessionStore.probeAccountContext` keeps the existing narrow path: current WebKit auth cookies transiently copied into an ephemeral `URLSession` -> GET `/api/auth/session` -> parse required `user.id` + transient local `accessToken` -> GET `/backend-api/accounts/check/v4-2023-04-27` with Bearer -> retain only required default account context in memory. b5 adds only safe `accountContextProbe.webData` total/matched cookie counts. No Cookie values, bearer values, Authorization headers or response bodies are logged or persisted.
- **State owner / shared dependencies**: Default persistent `WKWebsiteDataStore` remains the sole persistent auth-secret authority. `AuthSessionStore` owns safe auth evidence and Candidate in-memory account context. `/api/auth/session` bearer is transient/local only. Native `/auth/login` is historical diagnostic evidence, not an authentication state authority or current gate. UI titles remain consumers, not authorities.
- **Files / modules changed for b5**: `ChatGPTClient/Authentication/AuthWebViewController.swift`; `ChatGPTClient/Authentication/AuthSessionStore.swift`; `ChatGPTClient.xcodeproj/project.pbxproj`; `scripts/build_ipa.sh`; `.github/workflows/ios-foundation.yml`; checkpoint/durable docs. No unrelated refactor, retry, fallback, timer, watchdog, User-Agent spoof or Cloudflare bypass was added.
- **Parallel conflicts checked**: `docs/project/current/dev/` contains no other Active development checkpoint. PR #6 branch/base remain unique; b5 version/build/candidate/artifact identity is unique against b1-b4. Core roadmap remains serialized foundation -> auth -> protocol read.
- **Valid b5 CI/artifact evidence**: Authoritative push run `32932389742` at exact product/workflow source `c09f981171b02dc8a4f0d8ada4624bd779c68c2f` passed Xcode 16.4 build/inspect/upload. Logs explicitly show `SOURCE_COMMIT=c09f981171b0`, `DIAGNOSTICS_CANDIDATE=DEV-auth-bootstrap-0.1.0-b5`, `BUILD SUCCEEDED`, and `Candidate: DEV-auth-bootstrap-0.1.0-b5`. Artifact ID `9593649485`; artifact `ChatGPTClient-DEV-auth-bootstrap-0.1.0-b5`; IPA `ChatGPTClient-0.1.0-b5-dev-auth-bootstrap.ipa`; IPA SHA-256 `d9a22635cc6ac05d2ba09a0a627eaa74d38d1a690b5e9affe2f318d2aa204f15`; artifact ZIP digest `sha256:4ad6e95d4e30981aa63bb8bd401c0d4cd9acdddabbf83fab27b1f6fe54307066`.
- **Local artifact identity check**: Downloaded b5 artifact was extracted locally. Embedded Info.plist confirms app `0.1.0 (5)`, `DiagnosticsCandidate=DEV-auth-bootstrap-0.1.0-b5`, `DiagnosticsSourceCommit=c09f981171b0`, Release, deployment target 14.0. Local IPA SHA-256 recheck equals CI sidecar `d9a22635cc6ac05d2ba09a0a627eaa74d38d1a690b5e9affe2f318d2aa204f15`.
- **Rejected artifact**: First b4 artifact from run `32891478482` / artifact ID `9579620441` remains invalid because filename/artifact said b4 while embedded diagnostic candidate was b3. Do not use it.
- **Validation state**: b2/b3 retain accepted historical scopes. b4 is **Code written + CI passed + Artifact produced + Runtime/manual/real-device tested**, with result failed at native `/auth/login` Cloudflare 403 before account-context probe. b5 is **Code written + CI passed + Artifact produced; Runtime/manual/real-device not yet tested**. Account/workspace context remains Candidate / Unverified until b5 runtime evidence.
- **Current device-test acceptance**: Install exact b5 IPA. After WebKit reaches authenticated ChatGPT, b5 should show `网页登录成功 · 账户验证中`, then success title `登录会话 · 账户上下文通过`. On failure it shows `网页登录成功 · 账户上下文未通过` or `网页登录成功 · 账户验证失败`; export diagnostics once without repeatedly restarting. The decisive evidence is `accountContextProbe.end`: `stage=session` isolates `/api/auth/session`; `stage=accounts` isolates accounts-check. Success additionally requires `session.accountState=verified` and safe hashed exported `userID` / `accountID` references.
- **Next exact action**: User installs exact `ChatGPTClient-0.1.0-b5-dev-auth-bootstrap.ipa` from artifact ID `9593649485`, opens auth verification, waits for the final title, and exports b5 diagnostics. On receipt, first verify candidate/build/source identity, then interpret the direct account probe. If `/api/auth/session` itself returns 403, preserve that exact stage/status and do not add speculative bypasses.
- **Rejected / do-not-repeat**: Treating b4 as an account-context failure; treating WebKit login as lost because native `/auth/login` got 403; requiring native `/auth/login` success before the actual account/session request; system-browser fallback; speculative endpoint fallback/retry/timer chains; User-Agent spoofing; Cloudflare bypass; browser-script token extraction without new evidence; persistent copied Cookie/session/bearer storage; logging passwords, OAuth codes, tokens, Cookie values, Authorization values or response bodies.
- **Open questions / risks**: Direct native `/api/auth/session` may itself be Cloudflare-protected; Unknown until exact b5 device test. Accounts-check bearer acceptance/default-account response shape and workspace semantics remain runtime-unverified. Lower iOS versions, iPad and conversation protocol remain unverified.
