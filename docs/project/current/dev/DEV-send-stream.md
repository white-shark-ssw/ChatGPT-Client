# DEV-send-stream

## Status

**Active**

- **Work ID**: `DEV-send-stream`
- **Routing aliases / keywords**: `Send/Stream / 发送 / 流式回复 / 新对话 / Stop / reasoning / follow-tail`
- **Task**: Implement the first evidence-backed production text Send/new-conversation/streaming-response path, including exact response ownership, Stop integration, user-visible reasoning compatibility, follow-tail and new-chat identity handoff.
- **User intent / acceptance criteria**: Start Phase 9 now and continue autonomously through evidence-backed implementation, CI/package identity and a uniquely identified TrollStore IPA until a genuine real-device/human-only gate is reached. Do not guess current private ChatGPT protocol behavior; preserve one conversation authority and existing Stable b38 presentation behavior.
- **Baseline**: `main@34811877896ca88c6656be6676f5466a19931ce6`; predecessor `DEV-conversation-round-count` completed/merged Stable b38, PR #27 merge `9110c9e893e8a8665c7a58cf27bb42c65a39cc11`; exact accepted Runtime Candidate `DEV-conversation-round-count-0.1.0-b38`, tested product source `0d1801137e4ee2f5889ca718cd8b2e3612bdaa67`, Runtime Artifact `9708425762`.
- **Working branch / PR / head commit**: `dev/send-stream-20260829`; branch created from `34811877896ca88c6656be6676f5466a19931ce6`; activation commit `e82975dc04c210eaa5029eb611f6acd2f10eca57`; B1 durable preflight route head `2da49e98951511e10eacdaea5eeca01157219591`; B2 probe code head before this checkpoint `093c1675ca07a01560f9003b1f4132636dbd9b39`; PR not yet opened because the inherited workflow would build new code under reserved b38 identity.
- **Candidate identity**: **Allocated now**: `DEV-send-stream-0.1.0-b39` / `0.1.0 (39)`. Allocation evidence immediately before this checkpoint: current `main` unchanged at `34811877896ca88c6656be6676f5466a19931ce6`, no open PR, no other Active development checkpoint, no repository commit matching `b39`, and current Xcode source remains b38. b39 is reserved to this Work from this point forward and must never be reused by another Work.
- **Evidence**: Source audit confirms `ConversationDetail.currentNodeID` survives the read projection, `ConversationRepository` is still the sole account-scoped resident/read/recovery owner, `protectedResidentKeys()` currently protects detail operations + selected resident only, `ConversationDetailViewController` has no composer/response/follow-tail owner yet, `RootViewController` collapses to primary whenever `selectedConversationID == nil`, and `AuthTransientSession` exposes only completion-handler `URLSessionDataTask` delivery. `SEND_STREAM_PREFLIGHT.md` was missing from merged main and has now been restored/revalidated on this branch together with explicit START_HERE routing.
- **Current protocol research**: Recent 2026 public implementations/captures consistently identify current ChatGPT Web generation as `POST /backend-api/f/conversation`, generally preceded by Sentinel chat-requirements and `/f/conversation/prepare`; direct replay without valid browser-generated requirements/proof is reported to HTTP403. Current SSE evidence uses `data:` frames with protocol marker `"v1"`, patch/event objects, conversation/message identity fields, `message_stream_complete` in some captures and `[DONE]`; common text patches append `/message/content/parts/0`, and terminal message state may become `finished_successfully`. This is useful external structural evidence, **not accepted account/runtime proof**. Exact request/body/header requirements, Stop route and reasoning semantics for this product account remain Unknown/Unverified.
- **Evidence-backed decision**: Do **not** implement Sentinel/PoW/Turnstile bypass or replay captured proof credentials. The first Candidate is a visible diagnostic-only Web protocol probe that loads the official `chatgpt.com` page in the existing default WebKit data store and observes only privacy-safe structural metadata while the official page performs its own normal requirements flow. The probe does not become product chat transport, a hidden/shadow WebView, a second auth store, or persist any request/response body/token/header value.
- **Files / modules in scope**: future production `ChatGPTClient/Conversation/ConversationFeature.swift`, `ChatGPTClient/RootViewController.swift`; `ChatGPTClient/Authentication/AuthSessionStore.swift` only if accepted stream evidence later requires incremental authenticated transport. Current b39 evidence stage modifies `ChatGPTClient/Protocol/ProtocolReadProbe.swift` and `ChatGPTClient/SettingsViewController.swift` only, plus Candidate identity/workflow/docs.
- **State owner / shared dependencies**: `ConversationRepository` remains sole conversation/list/read/recovery/response authority; `AuthSessionStore` remains auth/account authority; default persistent `WKWebsiteDataStore` remains sole persistent auth-secret authority; `ConversationDetailViewController` remains viewport/scroll-presentation owner; b38 deterministic message presentation geometry/round-navigation contracts remain untouched in b39.
- **Frozen / do-not-touch**: No module is Frozen, but do not replace Stable b38 geometry/round-navigation architecture without new evidence. Do not create a second repository/global `isStreaming`, persist copied auth secrets, expose hidden chain-of-thought, add retry/timer/watchdog/fallback endpoints, bypass/solve service anti-bot protections in product code, or make Sync/Reload resend prompts.
- **Parallel conflicts checked against**: no Active peer checkpoint/open PR; predecessor merged; old rules preflight branch is planning-only. b39 uniqueness was rechecked immediately before allocation.
- **Completed**: startup/preflight/parallel/candidate audits; branch + rolling checkpoint; merged b38 source-owner audit; current protocol research; restored durable `SEND_STREAM_PREFLIGHT.md` + START_HERE route; added visible privacy-safe Send protocol probe in the existing Protocol diagnostic source and Settings entry. Compare against base shows only Protocol/Settings + docs changed; no Conversation/Root/Auth production owner change.
- **Validation state**: Code written for diagnostic evidence path; source/diff review in progress. **Candidate b39 allocated but product/workflow config not yet written and no CI/Artifact exists yet.** No Runtime/protocol acceptance yet.
- **Pending**: write b39 into both Xcode target configurations; update BUILD_TEST_INDEX allocation record; only then switch workflow branch/name/artifact to b39 so the first triggered CI cannot emit b38 identity; inspect CI and fix only deterministic compile/source issues; verify Artifact identity; hand b39 IPA to user for the minimal existing + new-chat official-Web probe and diagnostics export.
- **Next exact action**: Candidate write-chain D1: update Xcode `CURRENT_PROJECT_VERSION` 38→39 and `DIAGNOSTICS_CANDIDATE` to `DEV-send-stream-0.1.0-b39` without touching marketing version/deployment target; verify file; update BUILD_TEST_INDEX/checkpoint; then update workflow to `dev/send-stream-20260829` + b39 names, which intentionally becomes the first CI-triggering Candidate write.
- **Rejected / do-not-repeat**: Do not open PR or change workflow before Xcode identity is b39, because inherited PR/push packaging would mislabel new code as reserved b38. Do not reuse historical `/backend-api/conversation` Send assumptions; do not infer that current public reverse-engineered request is sufficient proof for this account; do not implement Sentinel/PoW/Turnstile bypass/replay; do not treat local `URLSessionTask.cancel()` as server Stop; do not allocate/reuse an emitted Candidate under corrected code; do not replay failed placeholder PR creation attempts.
- **Open questions / risks**: Exact current-account Send/new-chat request shape, server Stop route/ack, user-visible reasoning event semantics and final Detail reconciliation remain Unknown/Unverified. The diagnostic Candidate requires a real device/user message submission in the visible official page; that is the next genuine human-only evidence gate after CI/IPA production.

## Batch recovery point

Confirmed complete: branch/activation, **A source/protocol audit**, **B1 durable preflight restoration**, **B2 diagnostic probe code**. Do not replay those writes. **D1 Candidate chain starts now with b39 reserved**. Required order: (1) Xcode b39 identity, verify; (2) BUILD_TEST_INDEX allocation + checkpoint milestone, verify; (3) workflow branch/name/artifact b39, verify — this is intentionally the first write that can trigger Candidate CI; (4) inspect exact commit workflow run/job and Artifact; (5) if compile fails before Artifact, make only source-backed minimal correction and preserve evidence; if an Artifact is actually emitted, corrected product code must use a new Candidate/build. Runtime gate: user opens Settings → Send 协议探测（诊断）, performs one existing-chat and one new-chat Send through the visible official page, then exports diagnostics. Production implementation follows only from that exact evidence and uses a later unique Candidate. After interruption re-read this checkpoint + real branch/head and perform only missing deterministic writes; never blindly replay Candidate/package writes or touch another Work identity.