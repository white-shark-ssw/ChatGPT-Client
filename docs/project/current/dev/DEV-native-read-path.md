# DEV-native-read-path

## Status

**Active**

- **Work ID**: `DEV-native-read-path`
- **Routing aliases / keywords**: `官方 App UI 主框架与原生会话读取 / 原生会话读取 / 官方 App UI / native read path`
- **Task**: Build the official-App-style native main shell and establish production conversation list/detail/message read ownership.
- **User intent / acceptance criteria**: Replace the diagnostic landing screen with the first usable native read experience: official-style sidebar/main-chat shell, authenticated native conversation list, selected-conversation identity owned outside UI text, conversation-detail loading, and readable current-branch user/assistant messages. Keep b7 auth/protocol evidence boundaries and do not add speculative retry/fallback behavior.
- **Baseline**: `main` at `2ad48e5b1544dee4611edfaea85583e0a3aa81a8`; accepted runtime baselines `DEV-auth-bootstrap-0.1.0-b6` and `DEV-protocol-read-0.1.0-b7`; iPhone / iOS 17.0 runtime evidence; deployment target iOS 14.0.
- **Working branch / PR / head commit**: `dev/native-read-path-20260826`; PR not created; current product/config head before this checkpoint update `5b4b35287d31b0cf94e74ae927b912255a87ae2c`.
- **Candidate identity**: **Allocated** `DEV-native-read-path-0.1.0-b8`; version/build `0.1.0 (8)`; artifact name `ChatGPTClient-0.1.0-b8-dev-native-read-path.ipa`; branch-unique after re-check of all Active checkpoints and `BUILD_TEST_INDEX.md` (historical highest b7, no competing Active task).
- **Evidence**: b7 proved Plus/personal list GET and one detail GET with transient bearer + copied ephemeral WebKit cookies. First tested detail was 13,152,411 bytes / 2068 mapping nodes. Current implementation now establishes a separate production `ConversationRepository`; `ProtocolReadProbe` remains untouched and diagnostic-only.
- **Files / modules in scope**: `ChatGPTClient/AppDelegate.swift`; `ChatGPTClient/RootViewController.swift`; `ChatGPTClient/Conversation/ConversationFeature.swift`; `ChatGPTClient.xcodeproj/project.pbxproj`; `scripts/build_ipa.sh`; `.github/workflows/ios-foundation.yml`; authentication bridge usage from `AuthSessionStore`; diagnostics at conversation lifecycle boundaries; project checkpoint/durable docs.
- **State owner / shared dependencies**: `ConversationRepository` now owns production conversation summaries, selected conversation identity, current loaded detail and the current visible branch messages. It holds only an in-memory `AuthTransientSession`; `AuthSessionStore` remains auth/account-context owner and default WebKit data store remains the persistent auth-secret authority. `ProtocolReadProbe` remains diagnostic-only.
- **Frozen / do-not-touch**: No Frozen modules. Stable auth/account and protocol-read diagnostic contracts were not changed. No automatic retry, new headers, alternate endpoints or persistent copied credentials were added.
- **Parallel conflicts checked against**: Task creation scan found no Active checkpoints. Candidate allocation re-scan on this branch found only this task Active; b8 is unique and reserved to this task.
- **Completed**: Governance/session routing; project baseline review; branch creation; production conversation repository/state owner; authenticated list GET; selected identity ownership; detail GET with returned-identity check; current branch extraction by walking `current_node -> parent`; user/assistant text extraction; table-view virtualization instead of mounting all mapping nodes; native UIKit `UISplitViewController` sidebar/detail shell; settings/login/error/reload entry points; Xcode target membership; b8 build/artifact/CI identity wiring.
- **Validation state**: **Code written.** Candidate b8 allocated. No successful CI/artifact result recorded yet and no runtime/manual/real-device test yet. Local Xcode validation is unavailable in the current environment; GitHub Actions is the authoritative compile/package gate for this task.
- **Pending**: Observe b8 CI result; fix compile/runtime-shape issues from evidence only; if CI passes, record exact run/artifact/hash and then provide the b8 TrollStore candidate for real-device read/UI testing. Current visible parser intentionally ignores unsupported/non-text/non-user/non-assistant nodes instead of inventing semantics.
- **Next exact action**: Check the GitHub Actions run for current branch head `5b4b35287d31b0cf94e74ae927b912255a87ae2c`; if failed, inspect exact compiler/job evidence and make the smallest correction; if passed, record artifact identity and update durable project docs before real-device handoff.
- **Rejected / do-not-repeat**: No automatic session retry; no UA spoof/Cloudflare bypass; no speculative account/browser headers; no fallback conversation endpoints; no UI-title identity; no naive all-mapping view materialization; no reuse of `ProtocolReadProbe` as production repository; no hidden/shadow WebView for native reads.
- **Open questions / risks**: Exact user-visible content shapes beyond the simple text fields currently parsed remain runtime Unverified. Large mapping JSON still uses `JSONSerialization` for this first candidate; current-branch extraction bounds view materialization but does not yet prove parse-time/memory performance. Compact-width split navigation feel and message layout require iPhone/iOS 17.0 real-device acceptance.
