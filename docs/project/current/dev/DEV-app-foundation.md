# DEV-app-foundation

## Status

**Completing — runtime accepted**

- **Work ID**: `DEV-app-foundation`
- **Routing aliases / keywords**: `应用基础与日志系统 / 应用基础 / 日志系统 / diagnostics / app foundation`
- **Task**: 建立首个真实 iOS 原生应用基线、可审计构建身份与安全的本地结构化日志/诊断导出能力。
- **User intent / acceptance criteria**: 按 `DEVELOPMENT_PLAN.md` Phase 1 执行；产出最小真实 Xcode/iOS 应用骨架，建立可供 TrollStore 安装验证的 IPA 打包路径，从第一版即可记录有界持久化结构化日志并由用户主动导出脱敏诊断；必须区分 CI/Artifact 与实机证据。
- **Baseline**: base `main@bd9727e7a20c48c88944eff8a0f5fd0d23925ff6`; repository had no product source at this baseline. Final pre-merge recheck confirmed `main` remained at the same commit.
- **Working branch / PR / head commit**: `dev/app-foundation-20260826`; PR #5 `DEV-app-foundation: native app foundation and diagnostics`, base `main`; PR is mergeable. Product/workflow head `89b29434e4d81486d395b8ddb093a031f6f919a7` produced the accepted runtime candidate; later branch commits are documentation/checkpoint updates only.
- **Candidate identity**: `DEV-app-foundation-0.1.0-b1`; version `0.1.0`, build `1`; IPA payload artifact name `ChatGPTClient-0.1.0-b1-dev-app-foundation.ipa`; GitHub artifact logical name `ChatGPTClient-DEV-app-foundation-0.1.0-b1`.
- **Evidence**: GitHub Actions run `32876352123` on Xcode 16.4 produced artifact ID `9574034381`; extracted IPA verified arm64, version `0.1.0 (1)`, `MinimumOSVersion=14.0`, embedded source `89b29434e4d8`, SHA-256 `dcdefac9e508c5fd55c3c418fc0ea497c736f54fadc3b5e946300c5c1c032760`; final material PR head `aa3233de...` also passed run `32877096378`. User then installed/tested `DEV-app-foundation-0.1.0-b1` through TrollStore and reported no problems, including persistence after restart. Exported diagnostic JSON identifies candidate `DEV-app-foundation-0.1.0-b1`, version `0.1.0 (1)`, Release, deployment target 14.0, iPhone / iOS 17.0, source `89b29434e4d8`; it contains two launch sequences, a sample event from the first launch still present after the second launch, successful diagnostic exports, and no observed password/token/Cookie/Authorization/OAuth secret fields.
- **Files / modules in scope**: `ChatGPTClient.xcodeproj/`; `ChatGPTClient/` application shell/build metadata/diagnostics; `scripts/build_ipa.sh`; `.github/workflows/ios-foundation.yml`; `.gitignore`; this task's checkpoint and durable `docs/project/` truth updates.
- **State owner / shared dependencies**: `AppDelegate` owns foundation lifecycle/root setup; `AppBuildInfo` owns visible/exported build/runtime identity; `DiagnosticsLogger`/`DiagnosticsStore`/`DiagnosticsExporter` own diagnostic event/persistence/export state; Xcode target settings own version/build/deployment identity. No ChatGPT auth/session/protocol state is in scope.
- **Frozen / do-not-touch**: foundation modules may be marked Stable after this acceptance but are not Frozen; do not weaken logging privacy contract; do not raise iOS 14.0 merely because a newer SDK is used.
- **Parallel conflicts checked against**: no other Active development checkpoint existed on `main`, no duplicate branch/candidate identity, and `main` did not advance during this Work.
- **Completed**: native Swift/UIKit Xcode baseline; iOS 14.0 deployment target; app shell + Settings; build/runtime metadata; structured OSLog diagnostics; bounded persistent JSONL storage; trace/span timing; secret-field filtering; exported identifier hashing/redaction; user-triggered export; reproducible unsigned IPA packaging; scoped GitHub Actions CI; unique candidate identity; CI + artifact inspection; TrollStore real-device install/launch; settings/sample/export flow; cross-restart persistence; exported diagnostic metadata/privacy inspection.
- **Validation state**: **Code written** — yes. **Static/local checks passed** — no separate local Xcode environment; Xcode CI build/validation passed. **CI passed** — yes. **Artifact produced** — yes. **Runtime/manual/real-device tested** — yes, user-confirmed on iPhone iOS 17.0 via TrollStore and supported by exported diagnostics. **Stable/Frozen** — foundation is accepted for Stable status; not Frozen.
- **Pending**: update durable project docs from runtime evidence, remove this checkpoint, merge PR #5, then make `DEV-auth-bootstrap` the next serial development phase.
- **Next exact action**: finalize durable runtime evidence/status, remove this checkpoint under the completion rule, merge PR #5 against unchanged `main`, and record the merged foundation baseline.
- **Rejected / do-not-repeat**: no historical WebView chat baseline; no speculative retry/watchdog/fallback/remote telemetry; no auth secrets/full chat content in logs; no iOS 17.0 minimum by assumption; no CI/artifact-as-runtime claim; no auth/protocol work before foundation acceptance.
- **Open questions / risks**: lower-OS runtime compatibility below iOS 17.0 remains untested despite `MinimumOSVersion=14.0`; bundle ID remains not Frozen as a permanent signing/product identity; no unit/UI test target yet.
