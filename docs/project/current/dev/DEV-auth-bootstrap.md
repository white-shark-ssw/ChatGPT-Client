# DEV-auth-bootstrap

## Status

**Active**

- **Work ID**: `DEV-auth-bootstrap`
- **Routing aliases / keywords**: `网页登录 / Google 登录 / Google 登录验证 / 登录验证 / auth bootstrap`
- **Task**: Reproduce and verify the current ChatGPT web login path, specifically Continue with Google, on-device before native private-protocol work.
- **User intent / acceptance criteria**: Provide a testable iOS path that can open the current ChatGPT web login, attempt Continue with Google, preserve privacy-safe auth/navigation diagnostics, and establish real-device evidence of whether login succeeds or what current blocking/failure behavior occurs. Use the user-supplied 1024×1024 image as the app icon. Do not claim authenticated native protocol access until separately evidenced.
- **Baseline**: `main` at `836651a41e36feafcc2386939d70d673be6e3725`; accepted runtime foundation `DEV-app-foundation-0.1.0-b1`, version `0.1.0 (1)`, TrollStore tested on iPhone / iOS 17.0. `main` was rechecked after implementation and had not advanced.
- **Working branch / PR / head commit**: `dev/auth-bootstrap-20260826`; draft PR #6 `DEV-auth-bootstrap: web and Google login verification`; implementation/build-input milestone head `2ba8f8b610b20a776bbc35c9da7e46f2d228aa03` before this checkpoint refresh.
- **Candidate identity**: Reserved `DEV-auth-bootstrap-0.1.0-b2`; version `0.1.0 (2)`; intended IPA `ChatGPTClient-0.1.0-b2-dev-auth-bootstrap.ipa`; GitHub artifact name `ChatGPTClient-DEV-auth-bootstrap-0.1.0-b2`.
- **Evidence**: Current project docs require auth verification before protocol work. Historical user evidence says a prior Web-based IPA could sign in and the account uses Continue with Google; this is not a current runtime contract. Current Google OAuth guidance warns embedded user-agents such as `WKWebView` may be rejected, so this task intentionally captures the current real-device result before adding any alternate handoff.
- **Files / modules in scope**: `ChatGPTClient/Authentication/AuthWebViewController.swift`; `ChatGPTClient/RootViewController.swift`; existing diagnostics authority; `ChatGPTClient.xcodeproj/project.pbxproj`; `ChatGPTClient/Assets.xcassets/**`; `scripts/build_ipa.sh`; `scripts/reconstruct_app_icon.sh`; `scripts/assets/**`; `.github/workflows/ios-foundation.yml`; this checkpoint and build/test index.
- **State owner / shared dependencies**: `AuthWebViewController` currently owns only the embedded web-login verification UI/navigation lifecycle. No production authenticated-session owner has been established yet because login/session consumption is intentionally deferred until real-device evidence. Depends on Stable application shell and Stable diagnostics foundation.
- **Frozen / do-not-touch**: No modules are Frozen. Stable foundation modules were changed only where the current auth entry/build/icon requirements justified it. Do not implement native ChatGPT private protocol in this task before current authenticated-session evidence.
- **Parallel conflicts checked against**: No other Active development checkpoint existed when the task was created. The dedicated branch/candidate are unique. Core roadmap serial dependency remains foundation -> auth -> protocol read.
- **Completed**: Repository/source baseline inspected; dedicated branch and draft PR #6 established; minimal `WKWebView` bootstrap added at `https://chatgpt.com/auth/login`; root UI exposes `开始网页登录验证`; navigation request/response/finish/failure/new-window diagnostics record only privacy-safe host/destination/status metadata; no auth secret values are logged; candidate `DEV-auth-bootstrap-0.1.0-b2` / build 2 allocated; build and workflow identities updated; asset catalog wired; user-supplied app icon source encoded deterministically for the text-only repository write path and reconstructed before build with SHA-256 verification.
- **Validation state**: **Code written**. Local Xcode/static validation is not available in this environment. **CI pending** for the latest implementation/build-input head. **Artifact not yet verified**. **Runtime/manual/real-device not yet tested** for `DEV-auth-bootstrap-0.1.0-b2`. Not Stable/Frozen.
- **Pending**: Run/inspect GitHub Actions for the current branch head; if it passes, inspect the exact b2 artifact identity/checksum; then have the user install that exact candidate through TrollStore and attempt the normal Continue with Google path; capture the current success/block result and, when useful, a redacted diagnostics export. If embedded Google login is actually blocked, only then implement the smallest supported browser/auth handoff justified by that evidence.
- **Next exact action**: Inspect the latest GitHub Actions `iOS Auth Bootstrap` run for the current branch head. On failure, fix only the evidenced build/checksum/code issue. On success, record CI/artifact evidence and prepare exact real-device test instructions for `DEV-auth-bootstrap-0.1.0-b2`.
- **Rejected / do-not-repeat**: Do not assume old WebView login still works; do not assume WebKit/system-browser/URLSession sessions are interchangeable; do not log passwords, OAuth codes, tokens, Cookie or Authorization values; do not prebuild fallback chains; do not begin private-protocol implementation before current authenticated-session evidence; do not call CI/artifact success runtime proof.
- **Open questions / risks**: Current Google OAuth may reject the embedded user-agent; current ChatGPT login redirect/cookie/session behavior is not yet runtime-verified; a successful web login still would not by itself prove that native `URLSession` can consume that session. The app-icon reconstruction path must pass its checksum and Xcode asset compilation in CI before the icon is considered artifact-verified.
