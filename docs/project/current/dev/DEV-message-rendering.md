# DEV-message-rendering

## Status

**Active**

- **Work ID**: `DEV-message-rendering`
- **Routing aliases / keywords**: `消息渲染 / Markdown / 富文本 / filecite / 引用 / 链接颜色 / 用户链接`
- **Task**: Implement native user/assistant message presentation for link-only blue user text plus Markdown/code/table/file-citation display.
- **User intent / acceptance criteria**: User message ordinary text must use normal label color while only actual links are blue, including a bare URL immediately followed by Chinese text. Assistant body must stop exposing raw Markdown control syntax for headings/emphasis/lists/code/tables and must consume raw `filecite` control tokens into readable native presentation. Preserve the b112 assistant color fix. Citation target navigation is not to be guessed without authoritative annotation/resource evidence.
- **Baseline**: Stacked on `DEV-send-stream` Runtime-positive b112 branch head `50432b8743f3391a8174a3b7aae745298082d433`. Canonical b112 product/package/Artifact remain `3957b806f32f0995ceb9cf8f9487aba939f3b306` / `b5e3164721e01ceb1fe320ebd290bda79a921fc2` / `9975978222`; b112 Human Runtime diagnostics `sha256:36fd01529ee522fd0646f7bdf6e6f409dca3f55a4b17ff21c88e4e19d16e23b2` prove role-isolated assistant reuse color consistency on iPhone/iOS17 light appearance.
- **Working branch / PR / head commit**: `dev/message-rendering-20260906`; branch created from exact stacked dependency head `50432b8743f3391a8174a3b7aae745298082d433`; PR not created yet.
- **Candidate identity**: Not allocated.
- **Evidence**: User screenshot on canonical b112 shows assistant text color fixed but raw `**`, `###`, pipe-table markup and raw `filecite` token remain visible; the same screenshot shows a bare GitHub URL immediately followed by Chinese text causing the whole user bubble text to appear blue. Current source confirms `assistantBodyAttributedText` is plain attributed text; `userBodyAttributedText` uses Foundation inline Markdown and then colors every `.link` range system blue; `visibleText` retains only text/parts and no authoritative citation-resource model.
- **Files / modules in scope**: Primarily `ChatGPTClient/Conversation/ConversationFeature.swift`; Xcode project only when a runnable candidate/build is allocated; task checkpoint and durable presentation docs as evidence changes.
- **State owner / shared dependencies**: `ConversationRepository` remains authoritative content owner. This task owns presentation transformation only. It depends on unmerged b112 message-cell role-isolated reuse and b38 bounded long-message presentation geometry.
- **Frozen / do-not-touch**: Do not change protected Send/SSE/Web executor/recovery authority; do not merge user/assistant reuse pools; do not expose hidden reasoning/tool/system content; do not invent citation URLs/file navigation from opaque `turnNfileM` tokens; do not add third-party rendering dependency without explicit evidence/need.
- **Parallel conflicts checked against**: `DEV-send-stream` / PR #29 touches the same `ConversationFeature.swift` and is therefore an explicit stacked dependency, not an independent parallel task. PR #35 is research-only and does not touch product `ChatGPTClient/**` or product build identity. `main` remains `94f0c5777dad262cd1fb22be49082dbd92c962f2`. No `b113` entry exists in the stacked branch `BUILD_TEST_INDEX.md` at task creation.
- **Completed**: Read repository governance/START_HERE, architecture gap review and development plan; development plan explicitly reserves Phase 11 `DEV-message-rendering` for native Markdown/code/table/link/citation presentation. Verified b112 Runtime Positive baseline and current renderer source. Created isolated stacked branch and checkpoint.
- **Validation state**: Design/source evidence only; no product code written yet.
- **Pending**: Implement minimum native renderer, preserve long-message chunk correctness, allocate unique candidate, compile/CI/package, then Human Runtime on the exact screenshot scenario.
- **Next exact action**: Implement the smallest presentation-only renderer on this branch: conservative bare-URL detection for user messages; block+inline assistant Markdown rendering; readable non-interactive `filecite` presentation; Markdown-safe chunk boundaries. Then run exact diff/static/Simulator checks before Candidate allocation/package.
- **Rejected / do-not-repeat**: Do not treat b112 color fix as unresolved; do not color whole user markdown/link ranges based on an over-broad parsed bare URL; do not simply hide all citation evidence; do not implement guessed click-through citation resolution; do not render assistant Markdown by reparsing arbitrary syntax fragments split across existing 1200-character chunk boundaries.
- **Open questions / risks**: Current authoritative model retains only visible text and does not retain server citation annotation/resource objects, so first scope can present `filecite` cleanly but cannot claim authoritative source opening. Long-message chunking must not split Markdown blocks/fences/tables in a way that re-exposes syntax or corrupts styles.
