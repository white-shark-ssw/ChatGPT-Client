# DEV-app-foundation

## Status

**Active**

- **Work ID**: `DEV-app-foundation`
- **Routing aliases / keywords**: `应用基础与日志系统 / 应用基础 / 日志系统 / diagnostics / app foundation`
- **Task**: 建立首个真实 iOS 原生应用基线、可审计构建身份与安全的本地结构化日志/诊断导出能力。
- **User intent / acceptance criteria**: 按 `DEVELOPMENT_PLAN.md` Phase 1 执行；产出最小真实 Xcode/iOS 应用骨架，建立可供 TrollStore 安装验证的 IPA 打包路径，从第一版即可记录有界持久化结构化日志并由用户主动导出脱敏诊断；必须区分 CI/Artifact 与实机证据。
- **Baseline**: base `main@bd9727e7a20c48c88944eff8a0f5fd0d23925ff6`; repository had no product source at this baseline.
- **Working branch / PR / head commit**: `dev/app-foundation-20260826`; PR creation pending final base/conflict recheck; current branch includes product implementation plus proactive project-doc refresh. Latest CI-proven product/workflow head is `89b29434e4d81486d395b8ddb093a031f6f919a7`; later commits are documentation-only through this checkpoint update.
- **Candidate identity**: `DEV-app-foundation-0.1.0-b1`; version `0.1.0`, build `1`; IPA payload artifact name `ChatGPTClient-0.1.0-b1-dev-app-foundation.ipa`; GitHub artifact logical name `ChatGPTClient-DEV-app-foundation-0.1.0-b1`.
- **Evidence**: User started new task `应用基础与日志系统`; roadmap Phase 1 matches `DEV-app-foundation`; current source creates Swift/UIKit app foundation, build identity, bounded structured diagnostics and export; GitHub Actions run `32876352123` passed at `89b29434...` on Xcode 16.4 and produced artifact ID `9574034381`; prior run `32876300208` artifact was unpacked and verified as arm64, version `0.1.0 (1)`, `MinimumOSVersion=14.0`, candidate/source metadata present.
- **Files / modules in scope**: `ChatGPTClient.xcodeproj/`; `ChatGPTClient/` application shell/build metadata/diagnostics; `scripts/build_ipa.sh`; `.github/workflows/ios-foundation.yml`; `.gitignore`; this task's checkpoint and durable `docs/project/` truth updates.
- **State owner / shared dependencies**: `AppDelegate` owns foundation lifecycle/root setup; `AppBuildInfo` owns visible/exported build/runtime identity; `DiagnosticsLogger`/`DiagnosticsStore`/`DiagnosticsExporter` own diagnostic event/persistence/export state; Xcode target settings own version/build/deployment identity. No ChatGPT auth/session/protocol state is in scope.
- **Frozen / do-not-touch**: no Frozen product modules exist; do not implement auth/private protocol/chat features in this task; do not weaken logging privacy contract; do not raise iOS 14.0 merely because a newer SDK is used.
- **Parallel conflicts checked against**: new-task preflight found no other Active checkpoint and no duplicate branch/candidate; old `dev/foundation-roadmap-20260826` is completed planning history and was not reused. Final pre-PR/base/conflict recheck remains the next governance action.
- **Completed**: governance startup; baseline/tree verification; independent branch/checkpoint; Swift 5 + UIKit Xcode app target; iOS 14.0 deployment target; app shell + Settings; build/runtime metadata display; structured OSLog diagnostics; 2 MiB current + three-archive bounded JSONL persistence; trace/span timing; secret-field filtering; exported identifier hashing/redaction; user-triggered JSON diagnostic export; reproducible unsigned IPA packaging script; scoped GitHub Actions CI; unique version/build/candidate scheme; two successful CI builds; IPA artifact production/inspection; durable project profile/state/module/build/rule/technical-decision refresh.
- **Validation state**: **Code written** — yes. **Static/local checks passed** — no separate local Xcode environment is available in this session; Xcode's CI build/validation utility passed. **CI passed** — yes, latest relevant run `32876352123`. **Artifact produced** — yes, artifact ID `9574034381`. **Runtime/manual/real-device tested** — no. **Stable/Frozen** — no; modules remain Candidate.
- **Pending**: final base/parallel conflict recheck and PR identity; TrollStore install/launch on intended device; verify Settings opens, sample event writes, diagnostic JSON exports/share sheet works, exported metadata/redaction is correct, and persisted events survive relaunch; record runtime evidence and only then decide Stable/merge/phase completion.
- **Next exact action**: re-check current `main` plus all Active checkpoints for new overlap, create PR, then install the latest `DEV-app-foundation-0.1.0-b1` IPA through TrollStore and perform the foundation runtime checklist; feed the real result/export back into this checkpoint and durable docs.
- **Rejected / do-not-repeat**: no historical WebView chat baseline; no speculative retry/watchdog/fallback/remote telemetry; no auth secrets/full chat content in logs; no iOS 17.0 minimum by assumption; no CI/artifact-as-runtime claim; no independent auth/protocol work before this serial foundation gate is accepted.
- **Open questions / risks**: TrollStore's actual resign/install behavior for this unsigned candidate is not yet tested on the user's device; bundle ID is provisional rather than Frozen; no unit/UI test target yet; actual lowest-version device compatibility beyond compiled `MinimumOSVersion=14.0` requires runtime evidence.
