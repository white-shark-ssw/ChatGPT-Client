# DEV-auth-bootstrap

## Status

**Active**

- **Work ID**: `DEV-auth-bootstrap`
- **Routing aliases / keywords**: `网页登录 / Google 登录 / Google 登录验证 / 登录验证 / auth bootstrap`
- **Task**: Reproduce and verify the current ChatGPT web login path, specifically Continue with Google, on-device before native private-protocol work.
- **User intent / acceptance criteria**: Provide a testable iOS path that can open the current ChatGPT web login, complete Continue with Google, preserve privacy-safe auth/navigation diagnostics, verify session persistence, and establish evidence for how later native requests can consume authenticated state. Use the user-supplied 1024×1024 image as the app icon. Do not claim authenticated native protocol access until separately evidenced.
- **Baseline**: `main` remains at `836651a41e36feafcc2386939d70d673be6e3725`; accepted runtime foundation `DEV-app-foundation-0.1.0-b1`, version `0.1.0 (1)`, TrollStore tested on iPhone / iOS 17.0.
- **Working branch / PR / head commit**: `dev/auth-bootstrap-20260826`; draft PR #6 `DEV-auth-bootstrap: web and Google login verification`; b2 runtime-tested product/build-input source `809fa03e673afded87cb47fb755c998ab1b58e12`; b3 implementation head advances through the latest product/build commits before this checkpoint write.
- **Candidate identity**: b2 runtime evidence retained as `DEV-auth-bootstrap-0.1.0-b2`. New reserved candidate `DEV-auth-bootstrap-0.1.0-b3`, version `0.1.0 (3)`, intended IPA `ChatGPTClient-0.1.0-b3-dev-auth-bootstrap.ipa`, intended artifact `ChatGPTClient-DEV-auth-bootstrap-0.1.0-b3`.
- **Evidence**: b2 GitHub Actions run `32886019320` passed and produced artifact ID `9577612707`, IPA SHA-256 `426c5f9b6b5e71a41c3ca571abdc73951835a55dc902691d75030a781ee61465`. User real-device testing on iPhone / iOS 17.0 completed Continue with Google successfully. Supplied b2 diagnostics identify exact b2/source/runtime identity, show the initial Google flow reaching `accounts.google.com` then `auth.openai.com` and logged-in `chatgpt.com` HTTP 200, and after force-close/relaunch show `/auth/login` immediately reaching non-auth `chatgpt.com` HTTP 200 with no Google navigation. No error/fault auth events and no observed password/token/Cookie/Authorization fields were present.
- **Files / modules in scope**: `ChatGPTClient/Authentication/AuthWebViewController.swift`; new `ChatGPTClient/Authentication/AuthSessionStore.swift`; `ChatGPTClient/RootViewController.swift`; existing diagnostics authority; `ChatGPTClient.xcodeproj/project.pbxproj`; `ChatGPTClient/Assets.xcassets/**`; `scripts/build_ipa.sh`; `scripts/reconstruct_app_icon.sh`; `scripts/assets/**`; `.github/workflows/ios-foundation.yml`; checkpoint and durable project docs.
- **State owner / shared dependencies**: Default persistent `WKWebsiteDataStore` remains the only persistent auth-secret authority. New `AuthSessionStore` is the explicit safe authentication-state/native-probe owner and stores only web/native evidence state, not Cookie/token/header secrets. For the b3 probe it transiently copies only ChatGPT/OpenAI cookies into an ephemeral `URLSession`; the copy is not persisted. Depends on Stable application shell and Stable diagnostics foundation.
- **Frozen / do-not-touch**: No modules are Frozen. Do not add a system-browser fallback while the tested embedded route works. Do not duplicate WebKit authentication secrets into another persistent store. Do not treat b3 code/CI as proof that native session consumption works until the exact artifact is runtime tested.
- **Parallel conflicts checked against**: No other Active development checkpoint exists; dedicated branch/candidate remain unique; `main` has not advanced. Core roadmap serial dependency remains foundation -> auth -> protocol read.
- **Completed**: Minimal WebKit login bootstrap; privacy-safe auth navigation diagnostics; user app icon; b2 CI/artifact and real-device Google login; b2 force-close/relaunch persistence; diagnostic corroboration. For b3, code now adds explicit web/native auth evidence state ownership, automatic native-session probe after authenticated WebView navigation, ephemeral in-memory native cookie consumption, safe final-route/status diagnostics, visible probe result in the login screen title, build number 3/candidate b3 allocation, project source registration, workflow/artifact identity update and user-facing instructions.
- **Validation state**: b2 is **Code written + CI passed + Artifact produced + Runtime/manual/real-device tested for embedded Google login and WebKit persistence**. b3 is currently **Code written; CI pending; Artifact not yet verified; Runtime not tested**. Local Xcode/static checks are not available separately in this environment. Authentication/session is not Stable/Frozen yet.
- **Pending**: Run and inspect b3 GitHub Actions. Fix only evidenced compile/build issues. On success, verify exact b3 artifact/checksum and have the user install it while the WebKit login remains valid. Runtime acceptance for this step is `session.nativeState=verified` / UI title `网页登录成功 · 原生会话通过`, supported by safe final `/auth/login` probe destination/status. If b3 native consumption succeeds, next establish current account/workspace context before protocol-read implementation; if it fails, use the exact safe failure evidence rather than adding speculative fallback.
- **Next exact action**: Inspect the latest `iOS Auth Bootstrap` workflow run for the current b3 product head. On failure, repair only the concrete compiler/build issue. On success, record run/artifact/checksum and prepare exact b3 TrollStore test instructions.
- **Rejected / do-not-repeat**: Embedded Google necessarily requiring a system-browser fallback is rejected. WebKit session failing to persist across relaunch is rejected for b2 on the tested device. Do not add speculative auth fallback/retry/timer chains. Do not persist copied Cookie values outside WebKit. Do not log passwords, OAuth codes, tokens, Cookie values or Authorization values. Do not call native probe code or CI proof of authenticated native protocol access.
- **Open questions / risks**: Whether ChatGPT accepts the transient WebKit session in native `URLSession` remains unverified until b3 runtime test. Server acceptance may depend on more than cookies. Current account/workspace context and any headers needed for later private protocol requests remain Unknown / Unverified.
