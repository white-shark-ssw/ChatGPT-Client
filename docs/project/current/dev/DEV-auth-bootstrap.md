# DEV-auth-bootstrap

## Status

**Active**

- **Work ID**: `DEV-auth-bootstrap`
- **Routing aliases / keywords**: `网页登录 / Google 登录 / Google 登录验证 / 登录验证 / auth bootstrap`
- **Task**: Establish the current ChatGPT login/session bootstrap on real device, including WebKit persistence, native-session consumption evidence, and the account/workspace context required before native private-protocol read work.
- **User intent / acceptance criteria**: Provide a testable iOS login path, preserve privacy-safe diagnostics, prove session persistence and native consumption from current evidence, then establish one explicit account/workspace context owner before protocol-read work. Use the user-supplied app icon. Do not claim private conversation-protocol authentication until separately evidenced.
- **Baseline**: `main` base remains `836651a41e36feafcc2386939d70d673be6e3725`; accepted Stable foundation is `DEV-app-foundation-0.1.0-b1`, version `0.1.0 (1)`, TrollStore tested on iPhone / iOS 17.0.
- **Working branch / PR / head commit**: `dev/auth-bootstrap-20260826`; draft PR #6. PR base remains `main` at `836651a41e36feafcc2386939d70d673be6e3725`; branch head before this checkpoint write is `1d49b9bf62a44733dff05320e4b0159be6af0c52` and commits after b4 product source are documentation-only. b3 runtime-tested product source remains `0fcf040012c0698d0e3ce1628fec9865237eba3b`; identity-correct b4 runtime-tested product source is `33ea1b96f755bdf21fdd7691a9f1084a6d624908`.
- **Candidate identity**: b2 retained for web-login/persistence evidence. b3 `DEV-auth-bootstrap-0.1.0-b3` is accepted runtime evidence for the transient native `/auth/login` bridge under its tested conditions. b4 `DEV-auth-bootstrap-0.1.0-b4`, version `0.1.0 (4)`, is now a **runtime-tested failed diagnostic candidate**: it reproduced authenticated WebKit state but the native `/auth/login` gate was Cloudflare-blocked before account-context probing could start. New reserved next candidate is `DEV-auth-bootstrap-0.1.0-b5`, version `0.1.0 (5)`, intended IPA `ChatGPTClient-0.1.0-b5-dev-auth-bootstrap.ipa`, intended artifact `ChatGPTClient-DEV-auth-bootstrap-0.1.0-b5`.
- **Accepted b3 evidence**: User screenshot showed `网页登录成功 · 原生会话通过`. Supplied diagnostic export matched b3/build 3/source `0fcf040012c0`, iPhone / iOS 17.0; recorded `session.webState=authenticated`, 54 total / 35 matched WebKit cookies, `session.nativeState=verified`, and native probe final `chatgpt.com` HTTP 200 / `status=ok` in 1203.68 ms. This remains valid evidence for that tested time/route, but b4 proves the same `/auth/login` probe is not a stable prerequisite when Cloudflare challenges the route.
- **b4 exact runtime evidence**: User-supplied diagnostic export identifies candidate `DEV-auth-bootstrap-0.1.0-b4`, app `0.1.0 (4)`, source `33ea1b96f755`, Release, iPhone / iOS 17.0. Initial WebKit `https://chatgpt.com/auth/login` returned HTTP 403, then loaded `challenges.cloudflare.com` HTTP 200. WebKit subsequently submitted to non-auth `chatgpt.com`, received HTTP 200, finished there and recorded `session.webState=authenticated`. The native bridge then copied current WebKit context (46 total cookies / 27 ChatGPT/OpenAI matches) into an ephemeral `URLSession`, but `nativeSessionProbe.end` returned `chatgpt.com` auth destination HTTP 403 in 536.82 ms and set `session.nativeState=notAuthenticated`. The screenshot independently shows `网页登录成功 · 原生会话未通过`.
- **Critical interpretation**: b4 does **not** show that the persistent WebKit login was lost and does **not** show that `/api/auth/session` or the account-context request failed. The account probe never started: there is no `accountContextProbe.requested` / `session.accountState` event because `AuthWebViewController` gates `startAccountProbeIfNeeded()` behind native `/auth/login` verification. Current failure is therefore specifically the browser-oriented `/auth/login` route being an unreliable native prerequisite under the observed Cloudflare challenge.
- **Current account-context evidence basis**: Current live `https://chatgpt.com/api/auth/session` and `https://chatgpt.com/backend-api/accounts/check/v4-2023-04-27` remain the narrow evidence target already selected for this task. b4 code requests `/api/auth/session`, parses required `user.id` plus transient `accessToken`, then requests accounts-check with the bearer and stores only required default account context. Secrets and response bodies are not logged/persisted.
- **State owner / shared dependencies**: Default persistent `WKWebsiteDataStore` remains the only persistent auth-secret authority. `AuthSessionStore` owns safe auth evidence and candidate in-memory account context. The `/api/auth/session` bearer remains transient/local only. UI titles are consumers, not state authorities.
- **Files / modules in scope**: `ChatGPTClient/Authentication/AuthWebViewController.swift`; `ChatGPTClient/Authentication/AuthSessionStore.swift`; existing diagnostics authority; `ChatGPTClient.xcodeproj/project.pbxproj`; `.github/workflows/ios-foundation.yml`; `scripts/build_ipa.sh`; checkpoint and durable project docs.
- **Frozen / do-not-touch**: No modules are Frozen. Embedded login/persistence and the b3 historical native bridge evidence remain Stable for their accepted scope, not Frozen. Do not add browser fallback, persistent duplicate auth secrets, speculative retry/timer/watchdog chains, User-Agent spoofing, Cloudflare bypass logic, or broaden this task into conversation protocol without current evidence.
- **Parallel conflicts checked**: `docs/project/current/dev/` contains only this Active development checkpoint plus its README; no other Active development branch/candidate conflicts exist. PR #6 is still open/draft/mergeable; branch `dev/auth-bootstrap-20260826`; base `main` remains at recorded `836651a...`. New b5 identity is unique against `BUILD_TEST_INDEX.md` b1-b4.
- **Completed**: Web login, Google flow, persistence, privacy-safe diagnostics, app icon, b3 native-session bridge runtime acceptance for its tested route/time, current account/session endpoint evidence research, b4 account-context implementation and valid artifact production, and b4 real-device diagnosis isolating Cloudflare HTTP 403 at the legacy native `/auth/login` gate.
- **Rejected first b4 artifact**: Run `32891478482` / artifact ID `9579620441` remains invalid because its embedded candidate identity was b3 despite b4 filename/artifact naming. Do not use it.
- **Valid b4 artifact/runtime identity**: Authoritative push run `32891798350`; artifact ID `9579720453`; IPA `ChatGPTClient-0.1.0-b4-dev-auth-bootstrap.ipa`; IPA SHA-256 `f918b1f5762458e55e89a1f0d23e5c2bf46be11d7f4599c692627a07043dab03`; source `33ea1b96f755bdf21fdd7691a9f1084a6d624908`. This exact candidate produced the current failed runtime evidence above.
- **Validation state**: b2/b3 retain their accepted historical runtime scopes. b4 is **Code written + CI passed + Artifact produced + Runtime/manual/real-device tested**, with the runtime result **failed at native `/auth/login` Cloudflare 403 before account-context probe**. Account/workspace context itself remains Unknown / Unverified. b5 is identity-reserved only; code not yet written.
- **Minimal b5 direction**: Remove `/auth/login` native verification as the gate for the account-context diagnostic. Once WebKit has actually finished at authenticated non-`/auth` `chatgpt.com`, start the existing account-context probe directly. Add only privacy-safe cookie counts to `accountContextProbe` so the direct `/api/auth/session` request has enough evidence. Do not add retries/fallbacks or Cloudflare workarounds. This isolates the next real question: whether the current ephemeral native transport can call `/api/auth/session` and then accounts-check from the authenticated WebKit context.
- **Next exact action**: Implement that minimal direct account-context sequencing, allocate build/candidate b5 in Xcode/build script/workflow, run CI, verify exact source/candidate/artifact identity, then provide the b5 IPA for one real-device test. If `/api/auth/session` itself returns 403, preserve that exact stage/status as evidence and do not add speculative bypasses.
- **Rejected / do-not-repeat**: Treating b4 as account-context failure; treating current WebKit login as lost; requiring native `/auth/login` success before probing the actual account/session endpoint; system-browser fallback; historical endpoint guessing; speculative endpoint fallback chains; persistent copied Cookie/session/bearer storage; logging passwords, OAuth codes, tokens, Cookie values, Authorization values or response bodies.
- **Open questions / risks**: `/api/auth/session` may itself be Cloudflare-protected for native `URLSession`; this is Unknown until b5 directly probes it. Accounts-check bearer acceptance and response shape remain runtime-unverified. Workspace semantics beyond default account remain Unknown / Unverified. Lower iOS versions, iPad and conversation protocol remain unverified.
