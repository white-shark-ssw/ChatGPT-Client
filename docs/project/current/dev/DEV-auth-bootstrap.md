# DEV-auth-bootstrap

## Status

**Active**

- **Work ID**: `DEV-auth-bootstrap`
- **Routing aliases / keywords**: `网页登录 / Google 登录 / Google 登录验证 / 登录验证 / auth bootstrap`
- **Task**: Establish the current ChatGPT login/session bootstrap on real device, including WebKit persistence, native-session consumption evidence, and the account/workspace context required before native private-protocol read work.
- **User intent / acceptance criteria**: Provide a testable iOS login path, preserve privacy-safe diagnostics, prove session persistence and native consumption from current evidence, then establish one explicit account/workspace context owner before protocol-read work. Use the user-supplied app icon. Do not claim private conversation-protocol authentication until separately evidenced. User additionally requests a Settings control to clear accumulated local diagnostics during repeated device testing.
- **Baseline / resume identity**: `main` base remains `836651a41e36feafcc2386939d70d673be6e3725`; accepted Stable foundation is `DEV-app-foundation-0.1.0-b1`. Working branch `dev/auth-bootstrap-20260826`; draft PR #6 remains open/mergeable. Exact b5 product/workflow source is `c09f981171b02dc8a4f0d8ada4624bd779c68c2f`; branch commits after it through `2c1e72fc4b506b689362d65f56261f6e8bc48901` are documentation-only, verified by commit comparison. `main` has not advanced.
- **Candidate identities**: b2 retained for web-login/persistence evidence. b3 retained as historical native `/auth/login` success evidence under its tested conditions. b4 is runtime-tested and failed specifically at the native `/auth/login` Cloudflare gate before account-context probing. b5 `DEV-auth-bootstrap-0.1.0-b5`, version `0.1.0 (5)`, is now runtime-tested and establishes direct session/accounts transport plus a current response-shape mismatch. New reserved next candidate is **`DEV-auth-bootstrap-0.1.0-b6`**, version `0.1.0 (6)`, intended IPA `ChatGPTClient-0.1.0-b6-dev-auth-bootstrap.ipa`, intended artifact `ChatGPTClient-DEV-auth-bootstrap-0.1.0-b6`.
- **Accepted b3 evidence**: User screenshot showed `网页登录成功 · 原生会话通过`; diagnostics matched b3/build 3/source `0fcf040012c0`, iPhone / iOS 17.0, with 54 total / 35 matched WebKit cookies and native `/auth/login` final `chatgpt.com` HTTP 200. This remains valid route/time evidence only.
- **b4 exact runtime evidence**: User-supplied export identifies `DEV-auth-bootstrap-0.1.0-b4`, app `0.1.0 (4)`, source `33ea1b96f755`, Release, iPhone / iOS 17.0. WebKit `/auth/login` returned HTTP 403, then `challenges.cloudflare.com` HTTP 200, then WebKit reached non-`/auth` `chatgpt.com` HTTP 200 and recorded `session.webState=authenticated`. Native ephemeral `/auth/login` copied 46 total / 27 matching ChatGPT/OpenAI cookies but returned HTTP 403 in 536.82 ms and set `session.nativeState=notAuthenticated`. Account probe never started; b4 therefore isolated a failed prerequisite design, not an account-context endpoint failure.
- **b5 exact runtime identity**: User export `ChatGPTClient-Diagnostics-20260826-052835.json` identifies candidate `DEV-auth-bootstrap-0.1.0-b5`, app `0.1.0 (5)`, source `c09f981171b0`, Release, iPhone / iOS 17.0. This matches the authoritative b5 artifact identity.
- **b5 first direct-probe result**: At 05:27:47Z, after WebKit completed the Cloudflare flow and reached authenticated ChatGPT, `accountContextProbe.webData` recorded 46 total / 27 matching cookies. Direct `/api/auth/session` returned HTTP 403 in 555.66 ms and ended `stage=session`, `session.accountState=notAvailable`. This is route/time evidence that the direct session endpoint can still be challenge-sensitive immediately after a WebKit challenge.
- **b5 second direct-probe result**: At 05:28:25Z, WebKit reached non-`/auth` `chatgpt.com` HTTP 200. The account probe recorded 49 total / 30 matching cookies. `/api/auth/session` then returned HTTP 200 and produced the expected user id + transient access token (exported user id is hashed). The subsequent `/backend-api/accounts/check/v4-2023-04-27` request also returned HTTP 200, but b5 ended `stage=accounts`, `reason=missing_default_account` because the current parser requires `payload.accounts.default.account.id`.
- **Critical b5 interpretation**: The second b5 run proves the current ephemeral native transport can successfully perform `/api/auth/session` and the bearer-authenticated accounts-check request on the intended device. The remaining failure is **response parsing**, not HTTP authentication/transport. Current b5 source assumes an old/default-shaped object (`accounts.default.account.id`). Current 2026 public implementations of this same endpoint instead show `account_ordering` plus an `accounts` dictionary keyed by account identity, with the nested id field named `account_id`; this is the evidence basis for the next minimal parser correction. Do not add retries, alternate endpoints, browser token extraction, User-Agent spoofing or Cloudflare bypass.
- **b6 minimal parser direction**: Replace the unsupported `accounts.default.account.id` assumption with the current evidenced account ordering shape: require `account_ordering`, select its first usable account entry from `accounts`, and read nested `account.account_id`. Preserve optional `plan_type` / `structure`. If that exact shape is absent on device, record only safe structural counts/reason and stop; do not manufacture fallback endpoint chains.
- **Diagnostics clear requirement**: Repeated device tests have accumulated prior b1-b5 events in the bounded store. Add one Settings button to clear the existing DiagnosticsStore current JSONL plus rotated archives through the same diagnostics authority. Do not create another log store. Clearing should not clear WebKit login/session state and should not log secrets.
- **State owner / shared dependencies**: Default persistent `WKWebsiteDataStore` remains the sole persistent auth-secret authority. `AuthSessionStore` owns safe auth evidence and Candidate in-memory account context. `/api/auth/session` bearer is transient/local only. `DiagnosticsLogger` / `DiagnosticsStore` remain the sole diagnostics authority; the clear action is an operation on that owner, not a second state owner.
- **Files / modules in b6 scope**: `ChatGPTClient/Authentication/AuthSessionStore.swift`; `ChatGPTClient/Diagnostics/Diagnostics.swift`; `ChatGPTClient/SettingsViewController.swift`; `ChatGPTClient.xcodeproj/project.pbxproj`; `scripts/build_ipa.sh`; `.github/workflows/ios-foundation.yml`; checkpoint/durable docs. Diagnostics is Stable but not Frozen; touching it is justified by the user's explicit repeated-test log-clear requirement.
- **Parallel conflicts checked**: `docs/project/current/dev/` contains no other Active development checkpoint. PR #6 branch/base remain unique; b6 version/build/candidate/artifact identity is unique against b1-b5. Core roadmap remains serialized foundation -> auth -> protocol read.
- **Rejected artifact**: First b4 artifact from run `32891478482` / artifact ID `9579620441` remains invalid because filename/artifact said b4 while embedded diagnostic candidate was b3. Do not use it.
- **Valid b5 CI/artifact evidence**: Authoritative push run `32932389742` at exact product/workflow source `c09f981171b02dc8a4f0d8ada4624bd779c68c2f` passed Xcode 16.4 build/inspect/upload. Artifact ID `9593649485`; artifact `ChatGPTClient-DEV-auth-bootstrap-0.1.0-b5`; IPA `ChatGPTClient-0.1.0-b5-dev-auth-bootstrap.ipa`; IPA SHA-256 `d9a22635cc6ac05d2ba09a0a627eaa74d38d1a690b5e9affe2f318d2aa204f15`; artifact ZIP digest `sha256:4ad6e95d4e30981aa63bb8bd401c0d4cd9acdddabbf83fab27b1f6fe54307066`.
- **Validation state**: b2/b3 retain accepted historical scopes. b4 is Code written + CI passed + Artifact produced + Runtime/manual/real-device tested with native `/auth/login` Cloudflare 403. b5 is **Code written + CI passed + Artifact produced + Runtime/manual/real-device tested**: first direct session request 403 after challenge, second direct session request HTTP 200 and accounts-check HTTP 200, then parser failure `missing_default_account`. Account/workspace context remains Candidate / Unverified until corrected parsing succeeds on device. b6 is identity-reserved; code not yet written at this checkpoint.
- **Next exact action**: Implement the b6 current-response parser correction and diagnostics-clear control, update exact build/candidate identity to b6, run CI, verify artifact/source identity, then provide the b6 IPA for one real-device account-context test. Clear logs before the test if desired; after testing, export one fresh b6 diagnostic JSON.
- **Rejected / do-not-repeat**: Treating b5 accounts HTTP 200 as authentication failure; preserving the unsupported `accounts.default.account.id` shape after current evidence contradicts it; system-browser fallback; speculative endpoint fallback/retry/timer chains; User-Agent spoofing; Cloudflare bypass; browser-script token extraction without new evidence; persistent copied Cookie/session/bearer storage; logging passwords, OAuth codes, tokens, Cookie values, Authorization values or response bodies.
- **Open questions / risks**: Exact current accounts-check shape is evidenced publicly as `account_ordering` + keyed `accounts`, but must still be accepted from b6 real-device runtime. A fresh `/api/auth/session` request can return 403 immediately after Cloudflare challenge, as b5 first probe showed; do not add retry logic unless a later requirement/evidence justifies it. Workspace semantics beyond the selected account remain Unknown / Unverified. Lower iOS versions, iPad and conversation protocol remain unverified.
