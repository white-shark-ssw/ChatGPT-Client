# DEV-auth-bootstrap

## Status

**Active**

- **Work ID**: `DEV-auth-bootstrap`
- **Routing aliases / keywords**: `网页登录 / Google 登录 / Google 登录验证 / 登录验证 / auth bootstrap`
- **Task**: Establish the current ChatGPT login/session bootstrap on real device, including WebKit persistence, native-session consumption evidence, and the account/workspace context required before native private-protocol read work.
- **User intent / acceptance criteria**: Provide a testable iOS login path, preserve privacy-safe diagnostics, prove session persistence and native consumption from current evidence, then establish one explicit account/workspace context owner before protocol-read work. Use the user-supplied app icon. Do not claim private conversation-protocol authentication until separately evidenced.
- **Baseline**: `main` base remains `836651a41e36feafcc2386939d70d673be6e3725`; accepted Stable foundation is `DEV-app-foundation-0.1.0-b1`, version `0.1.0 (1)`, TrollStore tested on iPhone / iOS 17.0.
- **Working branch / PR / head commit**: `dev/auth-bootstrap-20260826`; draft PR #6. b3 runtime-tested product source remains `0fcf040012c0698d0e3ce1628fec9865237eba3b`. b4 account-context code/workflow reached `c924221fadda6a308ca04b1ae688c23ad423c34f`; packaging identity fix is `33ea1b96f755bdf21fdd7691a9f1084a6d624908`; this checkpoint write advances documentation only.
- **Candidate identity**: b2 retained for web-login/persistence evidence. b3 `DEV-auth-bootstrap-0.1.0-b3` is accepted runtime evidence for the transient native `/auth/login` bridge. Allocated candidate `DEV-auth-bootstrap-0.1.0-b4`, version `0.1.0 (4)`, intended IPA `ChatGPTClient-0.1.0-b4-dev-auth-bootstrap.ipa`, intended artifact `ChatGPTClient-DEV-auth-bootstrap-0.1.0-b4`.
- **Accepted b3 evidence**: User screenshot shows `网页登录成功 · 原生会话通过`. Supplied diagnostic export matches b3/build 3/source `0fcf040012c0`, iPhone / iOS 17.0; records `session.webState=authenticated`, 54 total / 35 matched WebKit cookies, `session.nativeState=verified`, and native probe final `chatgpt.com` HTTP 200 / `status=ok` in 1203.68 ms. This proves the tested ephemeral native `URLSession` can consume current WebKit-authenticated context for `/auth/login`; it does not prove private conversation endpoints.
- **Current account-context evidence basis**: Current live `https://chatgpt.com/api/auth/session` route exists and warns that returned information is sensitive. Recent 2026 evidence reports authenticated GET with browser cookies returns HTTP 200 with a `user` object and session bearer material. Current evidence also confirms `https://chatgpt.com/backend-api/accounts/check/v4-2023-04-27` exists (unauthenticated request returns 401), and recent ChatGPT-web tooling documents the current flow as session endpoint -> transient bearer -> accounts check, whose default account object contains account context. This evidence justifies one narrow diagnostic probe, not a general private-protocol model.
- **b4 code written**: `AuthSessionStore` adds `AuthAccountContextState` and in-memory `AuthAccountContext`. After the already-verified native auth bridge, `probeAccountContext` creates a fresh ephemeral session from current WebKit ChatGPT/OpenAI cookies, GETs `/api/auth/session`, parses only required `user.id` plus transient `accessToken` without logging/persisting the token, then GETs `/backend-api/accounts/check/v4-2023-04-27` with the bearer and records/stores only the required default account context. `userID`/`accountID` diagnostics use existing export hashing; response bodies and auth secrets are never logged. `AuthWebViewController` chains this probe after native-session verification and exposes a final account-context status title.
- **State owner / shared dependencies**: Default persistent `WKWebsiteDataStore` remains the only persistent auth-secret authority. `AuthSessionStore` owns safe web/native auth state, the tested transient native bridge, and b4 in-memory account context. The bearer parsed from `/api/auth/session` is a local variable used only for the immediate accounts-check request and is not retained or logged.
- **Files / modules in scope**: `ChatGPTClient/Authentication/AuthWebViewController.swift`; `ChatGPTClient/Authentication/AuthSessionStore.swift`; existing diagnostics authority; `ChatGPTClient.xcodeproj/project.pbxproj`; `.github/workflows/ios-foundation.yml`; `scripts/build_ipa.sh`; packaging/artifact identity files as required; checkpoint and durable project docs.
- **Frozen / do-not-touch**: No modules are Frozen. Embedded login/persistence and b3 transient native bridge are accepted Stable for tested scope/environment, not Frozen. Do not add browser fallback, persistent duplicate auth secrets, speculative retries/timers/watchdogs, or broaden b4 into conversation protocol.
- **Parallel conflicts checked against**: No other Active development checkpoint exists in `docs/project/current/dev/`; branch and b4 candidate are unique. PR #6 base remains `main` at `836651a41e36feafcc2386939d70d673be6e3725`. Core roadmap remains serialized foundation -> auth -> protocol read.
- **Completed**: Web login, Google flow, persistence, privacy-safe diagnostics, app icon, b3 native-session bridge runtime acceptance, current account/session endpoint evidence research, b4 minimal account-context implementation and unique build/candidate allocation.
- **b4 CI/artifact evidence**: Push run `32891478482` at `c924221fadda6a308ca04b1ae688c23ad423c34f` passed Xcode 16.4 build/inspect/upload and produced artifact ID `9579620441`, artifact name `ChatGPTClient-DEV-auth-bootstrap-0.1.0-b4`, IPA filename `ChatGPTClient-0.1.0-b4-dev-auth-bootstrap.ipa`, IPA SHA-256 `2739277493158c2170ef4b9d310210eb27996451a5d9a44347b40952ffdca55f`. **This artifact is rejected for runtime evidence**: job logs prove `scripts/build_ipa.sh` still passed `DIAGNOSTICS_CANDIDATE=DEV-auth-bootstrap-0.1.0-b3`, so its embedded candidate identity conflicts with the b4 filename/artifact. It must not be distributed or tested.
- **Concrete fix**: `scripts/build_ipa.sh` contained the evidenced stale default `CANDIDATE="${DIAGNOSTICS_CANDIDATE:-DEV-auth-bootstrap-0.1.0-b3}"`; commit `33ea1b96f755bdf21fdd7691a9f1084a6d624908` changes only that default to `DEV-auth-bootstrap-0.1.0-b4`. No retry/fallback or unrelated refactor added.
- **Validation state**: b3 is **Code written + CI passed + Artifact produced + Runtime/manual/real-device tested** for the transient native `/auth/login` bridge. b4 account-context code is **Code written**. Run `32891478482` is **CI passed + Artifact produced but artifact identity invalid/rejected**; therefore b4 has no valid runtime candidate yet and remains **Runtime not tested**. No separate local Xcode/static checks are available in this environment.
- **Pending**: Verify the post-fix push CI. Accept b4 for device testing only if logs show `DIAGNOSTICS_CANDIDATE=DEV-auth-bootstrap-0.1.0-b4`, output `Candidate: DEV-auth-bootstrap-0.1.0-b4`, exact source commit, successful build, and a fresh artifact/checksum. Runtime acceptance is final title `登录会话 · 账户上下文通过` plus diagnostics `session.accountState=verified`, successful `/api/auth/session` and accounts-check status, and exported hashed `userID` / `accountID` references. If the response shape differs, use exact safe failure evidence rather than adding fallback endpoints.
- **Next exact action**: Inspect the next push `iOS Auth Bootstrap` workflow caused by packaging fix `33ea1b96...`. On success, verify exact b4 embedded identity/source/artifact/checksum before distributing; on failure, repair only the concrete compiler/build/identity issue.
- **Rejected / do-not-repeat**: System-browser fallback requirement; WebKit persistence failure on tested device; native `/auth/login` session bridge failure on tested device; historical endpoint names as authority; speculative endpoint fallback chains; artifact `9579620441` as a runtime b4 candidate. Do not persist copied Cookie values or `/api/auth/session` bearer material. Do not log passwords, OAuth codes, tokens, Cookie values, Authorization values or response bodies.
- **Open questions / risks**: b4 account endpoint response shape and bearer acceptance remain runtime-unverified until an identity-correct b4 device test. Workspace semantics beyond the returned default account object remain Unknown / Unverified. Lower iOS versions, iPad and conversation protocol remain unverified.
